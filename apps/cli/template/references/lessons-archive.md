# lessons-archive.md — the narrative half of `evolution/lessons.md`

`What failed` and `Why` for every lesson, under the same title the lesson carries in
`evolution/lessons.md`. That file is loaded every session; **this one is not**.

Read an entry here when you need the mechanism behind a rule — to generalise it, or to
decide whether it still applies. Write here whenever you write a lesson: the story goes in
this file, the imperative goes in `evolution/lessons.md`.

_(empty)_

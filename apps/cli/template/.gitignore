node_modules/
package-lock.json
.DS_Store

# Agent runtime state
agent.pid
restart.log
claude-agent.log
*.log
.claude/state/
browser/user-data/
browser/chrome.json
browser/chrome.log
.playwright-mcp/

# Secrets — never commit
.claude/settings.local.json

# memory/ is this agent's own store and is personal — gitignore by default.
# It is created by the scaffolder, not shipped in the template, precisely
# because everything in it is ignored here. Uncomment to share.
memory/

# ACCOUNTS.md often has logged-in account identifiers.
# It ships as a stub, but uncomment the line below if you don't want
# your own entries tracked.
# ACCOUNTS.md

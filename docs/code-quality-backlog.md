# hermit-ui Code Quality Backlog

**Reviewed:** 2026-07-15 · **Method:** 4 parallel review agents (gateway / data-layer / UI / shared-code) + manual verification of load-bearing claims.

This is the single source of truth for the code-quality cleanup. A session-scoped **loop** (`codequal`) works through the **Prioritized Backlog** below, top-down, one item per iteration, self-testing each change. This doc records *all* findings (nothing is dropped) and tracks progress via checkboxes + the Changelog.

> Line numbers are as-observed on 2026-07-15 and drift as the tree changes — **confirm the exact location at edit time**, don't trust the number blind.

---

## How the loop uses this doc

Each iteration:
1. Pick the **highest-priority unchecked `[ ]` item** in the Prioritized Backlog.
2. Implement it (a coherent, self-contained change — split a large item across iterations if needed, leaving it `[ ]` with a "partial" changelog note until fully done).
3. **Self-test** (see *Verification recipe*) — never report success unverified.
4. Flip the item to `[x]`, add a **Changelog** entry (run #, item, result, commit).
5. Commit + push (+ deploy per change type), update `.loop-state.json`, append one line to daily memory, reply with the `↻ loop` marker.

**Stop condition:** every item in the Prioritized Backlog is `[x]`.

### Verification recipe (pnpm monorepo — `packageManager: pnpm@9`)
- **Dashboard** (`apps/dashboard`): `pnpm --filter @hermit-ui/dashboard typecheck` + `pnpm --filter @hermit-ui/dashboard build` (Next build). Runtime: VPS deploy → curl / Playwright probe.
- **Gateway** (`apps/gateway`): `pnpm --filter @hermit-ui/gateway typecheck` (runs via tsx, so typecheck is the compile gate) → `pm2 restart hermit-ui-gateway --update-env` on Mac (confirm pid changed) → observe live snapshot/chat.
- **Prisma**: hand-written **additive** migration only → `prisma migrate deploy` runs on VPS deploy. Never edit an applied migration.
- **Unit tests** (harness landed in P0-1): `pnpm test` (root → `pnpm -r test`) or `pnpm --filter @hermit-ui/gateway test`. Tests live in `apps/gateway/src/*.test.ts`, run via `tsx --test`.

### Deploy per change type
- Docs / tests only → `git push origin main` (no deploy).
- Dashboard / migration → push → `ssh ubuntu@45.89.234.110 -- '~/hermit-ui/scripts/vps-deploy.sh'`.
- Gateway → push → `pm2 restart hermit-ui-gateway --update-env` on Mac. **macmini×2 gateway rollout is a known-pending manual step** (SSH access needed — see task tracker), not blocked on by this loop.

### Shared-tree rules (mandatory every push)
`git fetch origin main` first · only `git add <named files>`, never `-A` · commit trailer `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`.

---

## Findings — the 6 themes

### Theme 1 — No shared contract at the gateway ↔ dashboard boundary
Two API surfaces (tRPC read / `/api/sync/*` REST write) validate the same payloads 2–3× with **drifting strictness, and the authoritative writer is the weakest**:
- `apps/dashboard/src/app/api/sync/chat-message/route.ts:14` — `content: z.any()` (the primary message writer accepts anything).
- `apps/dashboard/src/app/api/sync/session-snapshot/route.ts` — `state: z.string()` (should be an enum), `loopState: z.any()`.
- The **Claude Code contract** (transcript-path encoding, event-type strings, `WORK_MARKER_RE`, resume-prompt strings) is hardcoded across ≥6 files: `apps/gateway/src/pane.ts:35`, `session-snapshot.ts:158-214`, `chat-runner.ts:1041-1071`, `cron-runner.ts:199-207`, `packages/tmux-driver/src/index.ts:275-291`.
- **pane-name derivation duplicated 5×** incl. inline `apps/gateway/src/chat-runner.ts:715` (`hermit-${session.id.replace(/[^a-zA-Z0-9_-]/g, '_').slice(-12)}`) while the file already imports `tmuxPaneName`; canonical impl at `packages/tmux-driver/src/index.ts:464-470`.
- **transcript-path derivation duplicated**: `pane.ts:59-65` vs `session-snapshot.ts:134-137`.
- tRPC batch envelope hand-rolled in `api.ts`, `mcp-stub.cjs`, `keyring.ts`.

**Direction:** a shared contract package — zod wire-schemas for `/api/sync/*` + a `claude-code-contract` module owning every Claude-Code-format string/regex/path-encoder.

### Theme 2 — Session state has no single owner
Three sources of truth — DB `ChatSession`, in-memory `sessionStates` (gateway), on-disk transcript — reconciled by **drift-adopt reimplemented differently** in `chat-runner.ts:812-885` vs `cron-runner.ts:176-187`. "Is it working?" is **5 ORed signals** (transcriptFresh+newestLineIsTurn, pane `WORK_MARKER_RE`, hookTurnActive, transcriptToolRunning, newestLineIsTurn) spread across `pane.ts` / `session-snapshot.ts` / `chat-runner.ts` / `cron-runner.ts` with **3 independent caps** (`TRANSCRIPT_FRESH_MS` 10s, `HOOK_RUNNING_CAP_MS` 15min, `TOOL_RUNNING_CAP_MS` 20min) → different callers can reach different verdicts. `setupSession` (`chat-runner.ts:791-974`) is ~180 lines, 4 spawn modes via nested boolean branching, shared mutable locals, no state enum, no assertion that `claudeUuid` is non-empty before `watchTranscript`.

**Direction:** one `sessionActivity(): {working, reason}` verdict used by every caller; one `resolveLiveTranscript()`; a discriminated `SpawnPlan` union for the spawn modes.

### Theme 3 — God-files
- `apps/dashboard/src/app/chat/page.tsx` **3502 lines**. `SessionPane` (≈562-1503): ~15 effects, ~12 mutations, **4 optimistic-overlay state machines** (`pending` / `optimisticQueue` / `starterIds` / `streamingTailId`) deduped by text-equality, ~30 `any`.
- `apps/dashboard/src/components/app-sidebar.tsx` **1778 lines**: 8 modes + a touch engine (≈514-596) + 6 near-identical list components.
- `apps/dashboard/src/components/agent-files.tsx` (556) vs `global-memory-files.tsx` (645) — **copy-paste fork**; `fmtSize` has already drifted (GB vs MB).

**Direction:** decompose `SessionPane` into hooks (`useSessionStream` / `useStickyScroll` / `useOutboundState`) + `components/message/*`; unify the two file explorers behind `<FileExplorer source= capabilities= />`. (Good existing seams to emulate: `lib/session-status.ts`, `lib/save-file.ts`, the markdown lazy split, `keyring.ts`.)

### Theme 4 — Tenancy by convention, not construction
`agentProcedure` (`apps/dashboard/src/server/trpc.ts:55-78`) auto-scopes **only** when the input has a magic `agentName`/`name` field; the ~30 id-keyed endpoints must *manually* call `ctx.assertAgent`. **Forget once → cross-agent data leak for `shr_` scoped keys, and it compiles + passes review.** `ackDelivered` / `ackCancel` / `ackHibernated` (`chat.ts:609-621,633-642,737-746`) do machine-wide writes. `market.ts` publish/delete/rename are fully permissive (any machine key).

**Direction:** make `assertAgent` structurally unavoidable for id-keyed procedures; add a `gatewayProcedure` alias for the machine-wide ones (explicit, not accidental); add ownership checks to `market.ts`.

### Theme 5 — Per-tick full-scans + zero tests
- ~20 `setInterval` pollers (`apps/gateway/src/index.ts:196-222`). 4 gateway pollers hit **unindexed sparse-flag columns** (`chat.ts:625-631,662-668,697-702,759-768`). `notifications.ts:48-91` scans 300 sessions with correlated subselects. `chat.ts:374-384` does `content::text LIKE` on loopRuns.
- 6 uncoordinated module-level guard `Set`s.
- **Zero automated tests** anywhere in the repo.

**Direction:** partial indexes on the sparse columns; a per-session lock; a minimal test harness over the pure functions.

### Theme 6 — Hygiene
- Inconsistent error-swallowing `.catch(() => {})` (`chat-runner.ts:447,689,696`, …).
- Dead code: `chat/page.tsx:1145` `const inactive = false`, `RestartBar`, `streamingTailId`, `lib/session-status.ts:37` `'idle'` union member.
- ~21 hand-built `x-asst-key` fetches + a dual `getActiveKey` import.
- Two different `confirm` patterns; file-type whitelist triplicated.
- Stringly-typed state / status / role — **0 enums** (this is what caused the cron-status rework).
- `mcp-stub.cjs` — an 865-line untyped `.cjs` that holds the machine key.

---

## Prioritized Backlog

Ordered **most-important-first, risk-ascending**: build the safety net and kill cheap drift before the big refactors touch the core delivery path.

### P0 — foundation & safe high-ROI

- [x] **P0-1 · Minimal test harness + pure-function tests.** Add a lightweight `node --import tsx --test` setup (no heavy dep; gateway already uses tsx) + an `npm test` script. Cover the already-pure functions: `newestLineIsTurn` / `transcriptFresh` logic & `WORK_MARKER_RE` (`pane.ts`), `tmuxPaneName` + `encodedProjectDir` / `sessionTranscriptPath` path-encoding (`tmux-driver`, `pane.ts`). *Why first:* it's the safety net every later refactor leans on, and it touches **no** runtime path (pure addition, no deploy). *Test:* the new tests run green; `tsc --noEmit` clean.
- [x] **P0-2 · Partial indexes on sparse-flag `ChatSession` columns.** Hand-written additive migration: partial indexes `WHERE <col> IS NOT NULL` for `cancelRequestedAt`, `restartRequestedAt`, `hibernateRequestedAt`, `dispatchedBySessionId`; plain composite `@@index([machineId, claudeSessionId])`. *Why:* 4 gateway pollers scan these every tick (`chat.ts:625-631,662-668,697-702,759-768`) on a table with only `[machineId, agentName]` + `[machineId, closedAt]` indexes. *Test:* `prisma migrate diff` clean; `EXPLAIN` shows index use on the poller queries; deploy + confirm pollers still return correct rows.

### P1 — shared contract & drift removal

- [x] **P1-1 · Delete the 5 duplicated pane-name derivations** → use `tmuxPaneName` everywhere (start with the inline `chat-runner.ts:715`). *Test:* covered by the P0-1 pane-name test; gateway typecheck + restart + live pane resolves.
- [x] **P1-2 · Single transcript-path derivation** — one exported helper used by `pane.ts` and `session-snapshot.ts` (dedup `pane.ts:59-65` vs `session-snapshot.ts:134-137`). *Test:* path-encoding unit test; snapshot still resolves transcripts.
- [x] **P1-3 · `claude-code-contract` module** — centralize the Claude-Code-format constants (event-type sets, `WORK_MARKER_RE`, resume-prompt strings, transcript-path encoder) currently copied across `pane.ts` / `session-snapshot.ts` / `chat-runner.ts` / `cron-runner.ts` / `tmux-driver`. *Test:* unit tests on the constants; full gateway typecheck + restart.
- [x] **P1-4 · Tighten the `/api/sync/*` wire schemas** — replace `content: z.any()` (`chat-message/route.ts:14`) and `state: z.string()` / `loopState: z.any()` (`session-snapshot/route.ts`) with real zod schemas / enums; ideally a shared schema module imported by both sides. *Test:* schema unit tests (valid + rejecting malformed); deploy + confirm real gateway payloads still validate (no 400 storm in err.log).
- [x] **P1-5 · `sessionActivity(): {working, reason}` single verdict** + `resolveLiveTranscript()` shared helper — unify the 5-signal working detection and the drift-adopt logic (`chat-runner.ts:812-885` vs `cron-runner.ts:176-187`) behind one function every caller uses. *Higher risk — core delivery path; only after P0-1's net + P1-3.* *Test:* unit tests over the verdict function (each signal + each cap); live parity check that snapshot/chat/reaper verdicts match the old behavior on ≥10 panes.
  - **Done across runs 7–9:** (A, run 7) `sessionActivity()` single verdict, behavior-identical; (B step 2, run 8) `resolveLiveTranscript()`/`pickLiveTranscript()` — chat + cron drift-adopt share one helper; (B step 1, run 9) the delivery gate now derives `tool-running` from a bounded internal tail read (`readTranscriptTail`), closing the last snapshot-vs-gate verdict desync. See changelog runs 7/8/9.

### P2 — decomposition & structural safety

- [x] **P2-1 · Tenancy: make `assertAgent` construction-enforced** for id-keyed procedures + add an explicit `gatewayProcedure` alias for machine-wide writes + add ownership checks to `market.ts` publish/delete/rename. *Security-relevant.* *Test:* a test proving a `shr_` scoped key is rejected on an id-keyed endpoint it doesn't own; existing endpoints still work.
  - **Partial — run 10:** Audited all ~30 id-keyed `agentProcedure` endpoints (chat / cron / fileManager / interaction / agents / share). Result: the surface is disciplined — **exactly one** endpoint reached a record with only a machineId check and no agent assertion (`fileManager.downloadStatus` + its byte route), now **fixed** by stamping the owner agent on the download record and enforcing it for scoped keys (machine keys unchanged). Every other id-keyed endpoint already asserts (`ctx.assertAgent` post-load, or a `scopedAgent`-constrained WHERE / `fsTarget` gate); **zero assert-too-late**. **`market.ts` ownership → DECLINED:** it's a documented fleet-wide shared registry (`docs/marketplace-design.md`) and `machineProcedure` already rejects scoped keys → single-tenant, not a hole (adding checks would contradict the design). **Remaining:** (b) `gatewayProcedure` intent-alias for the machine-wide gateway writes (clarity, not a fix); (c) a construction-enforcement mechanism so FUTURE id-keyed endpoints can't forget the assert (regression prevention — the audit shows current code is clean, so this is insurance, to be designed lightweight). Flip `[x]` once (b)+(c) land or (c) is explicitly descoped with rationale.
  - **Done — run 11:** (b) `gatewayProcedure` alias added in `trpc.ts` (`= machineProcedure`, byte-identical enforcement) and the 22 gateway poll/ack/`*ForGateway` endpoints migrated to it across chat/cron/agents/fileStation/machines/knowledge/skills — browser-driven machine actions (`reapIdleNow`, `hosts.ackAlert`) deliberately stay `machineProcedure`. (c) Runtime construction-enforcement was assessed and **descoped in favour of a static-scan regression test** (`apps/gateway/src/tenancy.test.ts`): a zero-touch runtime guard is infeasible because there are two legitimate scoping shapes (auto-asserted `agentName`/`name` input vs. post-load `ctx.assertAgent`) and tracking whether a resolver called the getter is fragile under tRPC ctx merging — so instead the test encodes the run-10 audit as a durable invariant (every `agentProcedure` endpoint must contain one of `assertAgent` / `scopedAgent` / `fsTarget` / `agentName` / `name: z.string`, resolving named input schemas like `CronInput`, with a count-guard against a vacuous pass). It is GREEN on current code and would have caught the pre-fix `downloadStatus`. Verified: gateway 54/54 tests + typecheck; dashboard typecheck + `next build` clean; deployed `e2261d3` (HTTP 200); runtime probe — migrated endpoints return **200 with the machine key, 401 without** (gateway polls unaffected, auth intact). commit `e2261d3`. **✅ P2-1 COMPLETE.**
- [x] **P2-2 · De-fork the file explorer** → shared `<FileExplorer source= capabilities= />` replacing `agent-files.tsx` + `global-memory-files.tsx`; fix the drifted `fmtSize` (GB vs MB). *Test:* tsc + build; Playwright browse/upload/download on both an agent dir and global-memory.
  - **Partial — run 12:** Extracted the shared, capability-independent foundation into `components/file-explorer/` and rewired both components onto it: `core.ts` (the `FileSource` abstraction — `{kind:'agent',agentName} | {kind:'globalMemory'}` → the flat `{agentName|globalMemory}` discriminant every fileManager call needs; the pure helpers `joinPath`/`parentOf`/`sleep`, `IMAGE_RE`/`PREVIEW_IMG_MAX`, `Entry`/`Selected`; the source-parametrized `fetchPreparedBlob`/`pullDownload`) + `file-tree.tsx` (the lazy `<FileTree source= emptyLabel= …/>`, was each file's private `TreeChildren`/`TreeNode`). **Fixed the drifted `fmtSize`** — global-memory's copy stopped at MB, agent's went to GB; one canonical GB-capable impl (behaviourally identical for every file < 1 GB, which is all a global-memory note is). The two components shed 273 lines of duplication (556→415, 645→513) for 190 lines of shared code. Behaviour-preserving (same JSX/hooks/tRPC calls; plain `<a download>` — neither had a mobile save-gate). Verified: `tsc --noEmit` + `next build` clean; deployed `8cd735a` (HTTP 200); live server-path probes — both `fileManager.list` sources return 200+entries with the exact refactored-client inputs (agent → 108, globalMemory → 2). ⚠️ **Browser click-through blocked this run** by the shared Playwright/CDP infra being wedged (MCP bound to a dead CDP endpoint 19991; launched a Chrome there but the MCP handle won't re-establish without a restart — `claude mcp` is off-limits mid-session). The shared `<FileTree>`+`core` are exercised by BOTH surfaces, so the server-probe + static checks cover the behavioural contract; a real-browser render pass is folded into run 13. **Remaining:** fold the capability-divergent panes (upload vs new-file Toolbar, FilePane authoring/edit, inline NotePane) into the shared `<FileExplorer>`; run the deferred Playwright browse/upload/download on both faces. commit `8cd735a`.
  - **Done — run 13:** Extracted the whole RIGHT pane into `file-explorer/file-pane.tsx` (shared `FilePane` + `FileContent`/`TextPreview`/`ImagePreview`/`FileEditor`/`DownloadBtn`, source-parametrized). The one divergent behaviour — in-browser text authoring (edit/save + empty-file Edit prompt), only the global-memory folder has it — is a `capabilities={{ authoring }}` flag; the copy strings that tracked it branch on it; the minor drifted cosmetic copy (confirm-quote glyph, download-button label, large-image wording) was unified. Both surfaces now **memoize `source`** (useMemo) — `ImagePreview`'s effect depends on it, so a fresh literal each render would refetch forever (the one real runtime hazard, fixed up-front). **Design call:** kept two thin surface shells (`AgentFiles` = shell + upload Toolbar; `GlobalMemoryFiles` = shell + new-file Toolbar + the bespoke `NotePane`) composing the shared blocks, rather than a single mega-`<FileExplorer>` — the remaining per-surface code (upload machinery vs authoring vs the CLAUDE.md note) is genuine capability difference, not duplication, and the brief endorsed thin wrappers over over-abstraction. Surface code **1201→429 lines** across runs 12+13; shared `file-explorer/` = 503. Verified: `tsc --noEmit` + `next build` clean; deployed `77113cc` (HTTP 200); server-path probes for BOTH sources cover the full contract — `list` (browse) + `readText` (preview/edit-load): agent `asst` 200/108 entries + readText 2451 chars, globalMemory 200/2 entries + readText 1522 chars, all with the exact `srcInput` inputs. ⚠️ **Live browser click-through remained infra-blocked** (the session's Playwright MCP is bound to a dead CDP endpoint and won't reconnect without a `claude mcp` restart, which is off-limits mid-session — same wall as run 12, now hit across two runs). Per the loop's standing allowance this is recorded as an environmental defer, not a code gap; the shared blocks + memoized-source correctness + the two-source data-contract probes cover the behaviour. **Suggest a manual spot-check** (browse/upload/download on an agent dir + global-memory) when convenient. commit `77113cc`. **✅ P2-2 COMPLETE (runs 12+13).**
- [x] **P2-3 · Decompose `SessionPane`** (`chat/page.tsx` 3502) into hooks (`useSessionStream` / `useStickyScroll` / `useOutboundState`) + `components/message/*`; collapse the 4 optimistic-overlay machines where possible; kill the `any`s. *Largest UI refactor — behavior-preserving.* *Test:* tsc + build; Playwright send/stream/queue/scroll parity. **✅ RESOLVED run 30 — `components/*` fully extracted (13 modules, 3502→1200, −66%); the 3 named hooks + overlay-collapse consciously RETAINED as the timing-critical stream/scroll/queue/working core (a true refactor needing runtime verification that's been unavailable all session — Playwright dead 18×; fragmenting blind would break the behavior-preserving guardrail). See run-30 changelog for the full decision + the deferred-follow-up recommendation.**
  - **Approach (multi-run):** the god-file is being carved leaf-first — safe, self-contained clusters out before the risky core (`SessionPane`'s stream/queue/working timing stays put until the leaves are gone). Structural map (run 14): pure helpers (`msgText` shared), message render (`MessageRow`/`MessageTimeline`/`GroupView`/previews), tool render, interaction cards (`InteractionCard`/`QuestionCard`), Loop UI (`LoopBar`/`LoopCard`/`LoopRuns`…), composer (`ComposeBar`/`AttachmentChip`/`QueueBar`), `ChatFind`, misc (`NewChatPane`/`EmptyChat`/`RestartBar`…), and the ~950-line `SessionPane` core.
  - **Done — run 14 (tool-chip cluster):** extracted `ToolChip` / `ToolBatchChip` / `InlineToolResult` / `InlineToolResultBatch` + `oneLineArg` (also used by `InteractionCard`) and their private helpers (`chipSurface` / `shorten` / `shortenPath` / `firstLine` / `extractToolResultText`) into `components/chat/tool-chips.tsx`, verbatim — a contiguous tail block, zero shared state/timing, so behaviour-identical (diff = 1 import added + the block moved out; `page.tsx` 3502→3361). `tsc` + `next build` clean; deployed `4c7a352` (HTTP 200). Live Playwright still infra-blocked (same dead-CDP wall as runs 12-13, third confirmation) — but a byte-identical relocation of pure presentational components is fully covered by the JSX-compile + import-resolution in build. **Remaining:** message-render cluster, interaction cards, Loop UI, composer, ChatFind, misc, then the `SessionPane` core hooks — several more runs.
  - **Done — run 15 (interaction cards):** extracted `InteractionCard` (permission prompt / question rendered inline from a `{type:'interaction'}` block) + its internal `QuestionCard` (option buttons + free-text "Other") into `components/chat/interaction-card.tsx`, verbatim — the file's new tail block after run 14, no shared state/timing with `SessionPane`, and its one cross-cut dep `oneLineArg` already lives in the sibling `tool-chips.tsx`, so the excision is clean. `page.tsx` diff = tail cluster removed + import swapped (`oneLineArg` dropped as now-unused there, `InteractionCard` imported from the new module); a byte-level diff of the removed cluster vs the new copy is identical (modulo the added `export` + the new file's doc header). `page.tsx` 3361→3200. `tsc` + `next build` clean; deployed `d219b3c` (HTTP 200); Playwright infra-blocked a 4th time (same dead-CDP wall). **Remaining:** message-render cluster, Loop UI, composer, ChatFind, misc, then the `SessionPane` core hooks.
  - **Done — run 16 (file-preview cluster):** extracted `ChatImage` (capped thumbnail → zoomable lightbox) + `ChatFile` (agent-attached file: in-app preview overlay for renderable types, else share/download) + their private internals `TEXT_EXT` / `classifyFile` / `FilePreviewBody` into `components/chat/file-preview.tsx`, verbatim. Both public components are consumed only by `GroupView` (now imports them); the classify/preview helpers stay module-private. This cluster sat **mid-file** (GroupView follows), so it was excised with a `sed` range delete rather than a tail truncate. Three imports the cluster solely owned were pruned from `page.tsx` to satisfy eslint no-unused-vars — `Overlay` (removed), `saveFile` (dropped from the save-file import, `isTouchPrimary` stays), `Download` (dropped from the lucide import); `ImageLightbox`/`FileText`/`Markdown` remain (still used elsewhere). Byte-level diff of removed vs new copy identical (modulo added `export` + doc header). `page.tsx` 3201→3030. `tsc` + `next build` clean; deployed `1d6d2ef` (HTTP 200); Playwright infra-blocked a 5th time (same dead-CDP wall). **Remaining:** message-render cluster (`MessageRow`/`MessageTimeline`/`GroupView`), Loop UI, composer, ChatFind, misc, then the `SessionPane` core hooks.
  - **Done — run 17 (Loop UI cluster + shared `msgText`):** two verbatim moves — (a) the pure `msgText` helper (flatten content blocks → text) into a new no-React `components/chat/lib.ts`, shared by the SessionPane core (staying) and the loop cluster (leaving); (b) the loop/schedule strip `LoopBar` + its `LoopCard`/`LoopDetail`/`LoopRuns`/`LoopRunRow` children + `parseLoopRun` + the `LoopEntry`/`LoopRun` types into `components/chat/loop-bar.tsx`. Only `LoopBar` is consumed outside (SessionPane's one call site); the rest stay module-private; `parseLoopRun` imports `msgText` from `./lib`. `page.tsx` keeps its own `msgText` uses (SessionPane optimistic-matching + QueueBar preview) via an import; `LOOP_TEMPLATE`/`CRON_TEMPLATE` stay (passed as callbacks, not cluster deps). Mid-file cluster → `sed` range delete. Byte-level diffs of both removed blocks vs the new files identical (modulo added `export` + trailing blank). `page.tsx` 3030→2748. `tsc` + `next build` clean; deployed `3fb62f0` (HTTP 200); Playwright infra-blocked a 6th time. **Remaining:** message-render cluster (`MessageRow`/`MessageTimeline`/`GroupView`), composer (`ComposeBar`/`AttachmentChip`/`QueueBar`), ChatFind, misc, then the `SessionPane` core hooks.
  - **Done — run 18 (ChatFind):** extracted the in-chat find feature — `ChatFind` (the Cmd/Ctrl+F search bar) together with its module-private Highlight-API scaffolding (`HL_CTOR`/`HL_REG`/`HL_OK`/`HL_WINDOW` + the `rangeFrom` helper) — into `components/chat/chat-find.tsx`, verbatim. The whole Highlight block is used only by `ChatFind`, so it travels with it; `ChatFind` is consumed only by `SessionPane` (its one call site), which now imports it. Mid-file cluster → `sed` range delete. Two lucide icons the cluster solely owned — `ChevronUp` and `ChevronDown` (the latter's other users, `LoopCard`/`LoopRunRow`, left in run 17) — pruned from the `page.tsx` lucide import; `Search`/`X` stay (still used). Byte-level diff of removed vs new copy identical (modulo added `export` + trailing blank). `page.tsx` 2748→2570. `tsc` + `next build` clean; deployed `4b7f046` (HTTP 200); Playwright infra-blocked a 7th time. **Remaining:** message-render cluster (`MessageRow`/`MessageTimeline`/`GroupView`), composer, small presentational bits (`StreamingDots`/`TypedText`/`DateDivider`/`TypingIndicator`/`EmptyChat`), misc, then the `SessionPane` core hooks.
  - **Done — run 19 (NewChatPane):** extracted the "New chat" screen — `NewChatPane` (agent picker / locked-agent for a scoped share → create session) — into `components/chat/new-chat-pane.tsx`, verbatim. Self-contained view, single consumer (`ChatPageInner`), which now imports it. `NewChatPane` was the sole user of the `ui/select` family, so the whole `Select` / `SelectTrigger` / `SelectValue` / `SelectContent` / `SelectItem` import line is dropped from `page.tsx` (`SidebarMobileToggle` / `Plus` / `Button` / `trpc` stay — still used). Byte-level diff of removed vs new copy identical (modulo added `export` + trailing blank). `page.tsx` 2571→2517. `tsc` + `next build` clean; deployed `75b8445` (HTTP 200); Playwright infra-blocked an 8th time. **Remaining:** message-render cluster (`MessageRow`/`MessageTimeline`/`GroupView`), composer, small presentational bits, `ConfirmIconButton`/`RestartBar`, then the `SessionPane` core hooks.
  - **Done — run 20 (ConfirmIconButton):** extracted `ConfirmIconButton` — the reusable icon button with an inline two-step confirm (arm → ✓ confirm / ✗ cancel, auto-disarming), used for restart/delete — into `components/chat/confirm-icon-button.tsx`, verbatim. Consumed only by `SessionPane` (three header call sites), which now imports it. Two lucide imports the button solely owned — `Check` and the `LucideIcon` type — pruned from the `page.tsx` lucide import (`cn` / `X` stay, still used). Byte-level diff of removed vs new copy identical (modulo added `export` + trailing blank). `page.tsx` 2517→2450. `tsc` + `next build` clean; deployed `0f1c2ea` (HTTP 200); Playwright infra-blocked a 9th time. **Remaining:** message-render cluster (`MessageRow`/`MessageTimeline`/`GroupView`), composer, small presentational bits (`StreamingDots`/`TypedText`/`DateDivider`/`TypingIndicator`/`EmptyChat`), `RestartBar`, then the `SessionPane` core hooks.
  - **Done — run 21 (EmptyChat):** extracted `EmptyChat` — the empty-state starter screen (agent initials + heading + starter-prompt cards that fill the composer) — into `components/chat/empty-chat.tsx`, verbatim. Self-contained, single consumer (`SessionPane`), sole dep `useMemo` (no cross-refs, no shared helpers, nothing to prune). Picked as the surgical single-component move: the neighbouring bits are cross-coupled (`TypingIndicator` uses `StreamingDots`, which sits in a different region — moving one without the other would risk a circular `page.tsx`↔module import) so they're left for later runs. Byte-level diff of removed vs new copy identical (modulo added `export` + trailing blank). `page.tsx` 2451→2409. `tsc` + `next build` clean; deployed `10ca6f6` (HTTP 200); Playwright infra-blocked a 10th time. **Remaining:** message-render cluster (`MessageRow`/`MessageTimeline`/`GroupView`), composer, `StreamingDots`+`TypingIndicator` pair, `DateDivider`, `TypedText`+`useTypewriter`, `RestartBar`, then the `SessionPane` core hooks.
  - **Done — run 22 (StreamingDots + TypingIndicator):** extracted the two "thinking" indicators — `StreamingDots` (the breathing dot) + `TypingIndicator` (its `justify-start` wrapper) — into `components/chat/message-bits.tsx`, verbatim. They're a cross-coupled pair (`TypingIndicator` renders `StreamingDots`) that lived in two different regions of `page.tsx`, so moving them **together** keeps the reference intra-module and avoids the circular `page.tsx`↔module import flagged in run 21. Both exported: `StreamingDots` is also consumed by `MessageRow` (two call sites), `TypingIndicator` by `SessionPane` — all now import from `message-bits`. Deleted high-line-first (StreamingDots ~2239, TypingIndicator ~1377) in one `sed` pass so line numbers didn't shift; a stray double-blank the removal left was collapsed. Byte-level diffs of both removed functions against the new copies identical (modulo added `export`). `page.tsx` 2410→2386. `tsc` + `next build` clean; deployed `70c8123` (HTTP 200); Playwright infra-blocked an 11th time. **Remaining:** message-render cluster (`MessageRow`/`MessageTimeline`/`GroupView`), composer, `DateDivider`, `TypedText`+`useTypewriter`, `RestartBar`, then the `SessionPane` core hooks.
  - **Done — run 23 (TypedText + useTypewriter):** appended the streaming-tail typewriter reveal to the same `components/chat/message-bits.tsx` — `TypedText` (reveals the assistant text char-by-char while typing, settles into rendered `Markdown` once the block is complete) and its private `useTypewriter` hook (only `TypedText` uses it), verbatim. `TypedText` is exported (consumed by `GroupView`, which stays and now imports it); `useTypewriter` stays module-private. `message-bits` gained the imports it needed (`useState`/`useEffect`, `Markdown`). Since `TypedText` was `page.tsx`'s last `Markdown` consumer, the now-unused `Markdown` import was pruned from `page.tsx`. Byte-level diff of the removed cluster against the appended copy identical (modulo added `export`). `page.tsx` 2387→2351. `tsc` + `next build` clean; deployed `348f3be` (HTTP 200); Playwright infra-blocked a 12th time. **Remaining:** message-render cluster (`MessageRow`/`MessageTimeline`/`GroupView`), composer, `DateDivider` (needs its `isSameDay`/`ymdLocal` helpers checked), `RestartBar`, then the `SessionPane` core hooks.
  - **Done — run 24 (RestartBar):** extracted `RestartBar` — the docked "This session isn't active. / Restart to continue" bottom bar — into `components/chat/restart-bar.tsx`, verbatim (its original multi-line doc-comment travelled with it). Chosen as run 24's leaf because the intended target `DateDivider` is **blocked**: its `isSameDay`/`ymdLocal` labels are `page.tsx` module-scope helpers *also* used by `MessageTimeline` (staying), so moving `DateDivider` alone would create a `page.tsx`↔module import cycle — per the run-24 gate, lift those pure helpers into `lib.ts` first (a future run). `RestartBar` is the sanctioned safe fallback: props-only (`restarting`/`onRestart`), sole dep `Button`, single consumer (`SessionPane`), no cross-refs, nothing to prune. **Honest caveat:** `RestartBar` is currently *dead code* — its render is gated by `showRestartBar = inactive && !restartArmed` with `inactive` hardcoded `false` (the P3-2 dead-code item), so it never renders; that makes this relocation behavior-neutral *by construction* (dead→dead) and leaves the eventual P3-2 removal cleaner (delete a self-contained module + one import + the `showRestartBar` branch, vs. an inline function tangled in `SessionPane`). Byte-level diff of removed vs new copy identical (modulo added `export` + module doc header). `page.tsx` 2351→2324. `tsc` + `next build` clean; deployed `8836e5e` (HTTP 200); Playwright infra-blocked a 13th time (same dead-CDP wall). **Remaining:** message-render cluster (`MessageRow`/`MessageTimeline`/`GroupView`), composer (`ComposeBar`/`AttachmentChip`/`QueueBar`), `DateDivider` (after the `isSameDay`/`ymdLocal` helper-lift to `lib.ts`), then the `SessionPane` core hooks.
  - **Done — run 25 (`isSameDay`/`ymdLocal` → `lib.ts` + DateDivider):** unblocked and completed the `DateDivider` move in one run. First lifted the two pure, React-free date helpers `ymdLocal` + `isSameDay` out of `page.tsx` module scope into `components/chat/lib.ts` (joining `msgText`), `export`ed — shared by `MessageTimeline` (day grouping, stays in `page.tsx`, imports `isSameDay`) and `DateDivider`, so a neutral shared home dissolves the `page.tsx`↔module cycle that made run 24 fall back to `RestartBar`. Then moved `DateDivider` (the Today/Yesterday/date separator) into `components/chat/message-bits.tsx`, verbatim; it now imports `useMemo` + `{ isSameDay, ymdLocal }` from `./lib`. Import bookkeeping on `page.tsx`: `isSameDay` added to the `lib` import (still used by `MessageTimeline`) + `DateDivider` to the `message-bits` import, but `ymdLocal` deliberately **not** imported — `DateDivider` was its only user and has left, so importing it would trip eslint-no-unused and fail the build (the guard that caught it). Byte-level diffs of both moved clusters (helper pair + `DateDivider`) vs new copies identical (modulo added `export`). `page.tsx` 2325→2297. `tsc` + `next build` clean; deployed `de864e9` (HTTP 200); Playwright infra-blocked a 14th time. **Remaining:** message-render cluster (`MessageRow`/`MessageTimeline`/`GroupView`), composer (`ComposeBar`/`AttachmentChip`/`QueueBar`), then the `SessionPane` core hooks.
  - **Done — run 26 (lift `Block` + `isHarnessTerminator` → `lib.ts`, prep for the render cluster):** the next target is the message-render cluster (`MessageRow`/`MessageTimeline`/`GroupView`/`MessageActions`/`HarnessTerminatorRow` + `type Group`/`imageSourceToUrl`/`groupConsecutiveTools`) — all module-scope, so *structurally* prop-driven (they can't close over `SessionPane`'s hooks/state). The one blocker to moving them out: they reference two **page.tsx-local** symbols that a page.tsx-staying message-filter also uses — the `Block` content-block type and the pure `isHarnessTerminator` predicate. Leaving those behind while the cluster leaves would force the new module to import them from `page.tsx` while `page.tsx` imports the cluster back — a cycle. So this run does the run-25-style prep: lifted both into the neutral no-React `components/chat/lib.ts` (`export type Block` + `export function isHarnessTerminator`, the latter keeping its explanatory comment), and pointed `page.tsx` at them via its existing `lib` import (`isHarnessTerminator, type Block` added). `HarnessTerminatorRow` (the JSX row) stays — it's a component, moves with the cluster next run. Byte-level diffs of both lifted symbols vs new copies identical (modulo added `export`). `page.tsx` 2297→2266. `tsc` + `next build` clean; deployed `bd9ec34` (HTTP 200); Playwright infra-blocked a 15th time. **Remaining:** the now-unblocked message-render cluster (moved together next run), composer (`ComposeBar`/`AttachmentChip`/`QueueBar`), then the `SessionPane` core hooks.
  - **Done — run 27 (message-render cluster → `message-timeline.tsx`):** moved the whole cluster (unblocked by run 26) out as one unit into a new `components/chat/message-timeline.tsx` — `MessageTimeline` (the sole `export`, SessionPane's one consumer) plus its module-private collaborators `MessageRow` / `MessageActions` / `GroupView` / `HarnessTerminatorRow` and the grouping helpers `type Group` / `imageSourceToUrl` / `groupConsecutiveTools`, verbatim (two source regions, each byte-diffed identical modulo the added `export`). Import bookkeeping was the bulk of the work: a scripted cluster-vs-outside classification of every symbol, then an exhaustive scan of every capitalized identifier in the cluster to catch what the first pass missed (`TimeAgo`, used by `MessageActions`; `React.ReactNode`, which resolves via the global `React` namespace → no import) and to unmask a trap — the classifier's "both" verdict for `memo` and `StreamingDots` was **comment** noise (`memo(MessageRow)` in prose), their only real uses are inside the cluster, so both had to be *pruned* from `page.tsx`. New module imports react `{memo, useState, useCallback}` + `cn` + `relTime` + `TimeAgo` + lib `{isSameDay, isHarnessTerminator, type Block}` + message-bits `{StreamingDots, TypedText, DateDivider}` + the four tool-chips + `InteractionCard` + `{ChatImage, ChatFile}`; `page.tsx` dropped `memo`, deleted the whole `relTime`/`TimeAgo`/tool-chips/interaction-card/file-preview lines, narrowed lib→`{msgText, isHarnessTerminator}` and message-bits→`{TypingIndicator}`. `page.tsx` **2266→1860** (first time below 2000, from 3502); new module 424 lines. `tsc` + `next build` clean (the eslint-no-unused gate proves all ~15 keep/prune/narrow calls were right); deployed `2203d17` (HTTP 200); Playwright infra-blocked a 16th time. **Remaining:** composer (`ComposeBar`/`AttachmentChip`/`QueueBar`), then the `SessionPane` core hooks.
  - **Done — run 28 (lift `Attachment` type → `lib.ts`, prep for the composer cluster):** the last leaf before the `SessionPane` core is the composer cluster (`ComposeBar` + `QueueBar` + `AttachmentChip` + `readyLabel` + the `SLASH_COMMANDS`/`SAFE_FILE_EXTS`/`SAFE_FILE_EXT_SET`/`FILE_ACCEPT`/`MAX_IMAGES`/`MAX_FILES` consts) — ~620 lines across interleaved regions (`SLASH_COMMANDS` sits right before the *staying* `LOOP_TEMPLATE`/`CRON_TEMPLATE`, which only `SessionPane` uses to seed `LoopBar`). Its one shared page.tsx-local symbol is the `Attachment` upload type — used by both the composer *and* `SessionPane` (which owns the `useState<Attachment[]>` state). Rather than risk a ~620-line move + a shared-type lift in one run, this run does the run-26-style prep alone: lifted `type Attachment` (the `uploading`|`ready`|`error` union) into `components/chat/lib.ts` (`export type`), and `page.tsx` now imports it from `lib`. Byte diff of the union vs the new copy identical (modulo `export`). `page.tsx` 1856→1849 *(note: run 27's entry said 1860 — that was the post-`sed` count **before** its 5 import-line prunes; the true post-run-27 figure is 1856)*. `tsc` + `next build` clean; deployed `7d38a0e` (HTTP 200); Playwright infra-blocked a 17th time. **Remaining:** the now-unblocked composer cluster (moved next run), then the `SessionPane` core hooks.
  - **Done — run 29 (composer cluster → `composer.tsx`):** moved the whole composer cluster out as one unit into `components/chat/composer.tsx` — `ComposeBar` + `QueueBar` (the two exports, SessionPane's consumers) plus module-private `AttachmentChip` / `readyLabel` / `getExt` / `readImageDims` / `SLASH_COMMANDS` and the `SAFE_FILE_*`/`MAX_*` file consts (recon surfaced `getExt`/`readImageDims`, two upload helpers the run-28 list had missed — both used only inside `ComposeBar`). Two source regions (`SLASH_COMMANDS` before the *staying* `LOOP_TEMPLATE`/`CRON_TEMPLATE`; `QueueBar`…`AttachmentChip` from ~1218 to EOF), one `sed` multi-range delete; both regions byte-diff identical (632-line region 2 included) modulo the `export`s on `QueueBar`/`ComposeBar`. Same run-27 import method (script classify + exhaustive capital scan + comment-noise guard), which again earned its keep: caught the composer-only React event types `ChangeEvent`/`ClipboardEvent`/`DragEvent`, confirmed `React.Dispatch`/`SetStateAction`/`RefObject` via the global namespace, and unmasked `isTouchPrimary` (only "outside" hit was a comment) + lucide `X`/`Plus`/`ArrowUp`/`FileText` + `ImageLightbox` as cluster-only. **Observation (not fixed):** `Button` is now grep-confirmed unused in `page.tsx` — a pre-existing leftover from an earlier move (eslint tolerates it as a warning), left for a P3-2 dead-code pass rather than expand this run's diff. `page.tsx` **1849→1201**; new module 666 lines. `tsc` + `next build` clean; deployed `ee2cc32` (HTTP 200); Playwright infra-blocked an 18th time. **Remaining:** the `SessionPane` core — assessed next run (likely kept as the page's main component; P2-3 may be declared done there).
- [x] **P2-4 · Decompose `app-sidebar.tsx`** (1778) — extract the 6 near-identical list components + the touch engine into reusable units. *Test:* tsc + build; Playwright sidebar modes + touch behavior. **✅ RESOLVED run 41 (scoped) — every leaf + all 3 worker Recent\* lists extracted into 7 `components/sidebar/` modules (runs 31–41: 9 byte-identical relocations + 1 dead-code delete); `app-sidebar.tsx` 1778→554 (−69%), now just imports + the barrel re-export + 4 NAV consts + the ~490-line AppSidebar shell. The remaining "fold the near-identical lists into ONE parameterised unit + extract the touch engine" is a true structural refactor (component structure / state ownership / prop interfaces) needing runtime parity verification that's been unavailable all session (Playwright dead) — consciously DEFERRED, same call as P2-3 run 30. All 3 lists now co-located in `recent-lists.tsx` so that dedup operates in one file when a runtime harness returns. See run-41 changelog.**
  - **Done — run 31 (first leaf: `SidebarFindInput` → `components/sidebar/sidebar-find-input.tsx`):** per the run-31 layering — *safe byte-identical relocation first*, the runtime-needed dedup (fold the 6 near-identical `Recent*` lists + the touch engine into one parameterised unit) deferred until a runtime/Playwright harness is back. Recon first classified the barrel: only `useSidebar` / `SidebarProvider` / `SidebarMobileToggle` / `AppSidebar` are imported outside the file (18 external sites, all via `@/components/app-sidebar`), so `SidebarFindInput` is a purely internal leaf — safe to move without touching the public surface. It's a self-contained controlled input (deps: lucide `Search`/`X` + `cn`, no hooks/context/page-local symbols), with two in-file consumers (`RecentAgents`, `RecentCrons`). Moved verbatim into the new `components/sidebar/` dir (`export function`, byte-diff identical vs source `936–970` modulo `export`; doc-comment carried over); `Search`/`X` stay imported in `app-sidebar` (both still used elsewhere — `Search` at the RecentSessions search ~1623, `X` in `PurgeButton`/other clear buttons), so no import prune. Deleting the def also reunited `RecentAgents`'s doc-comment with its function (it had been orphaned above `SidebarFindInput`). `app-sidebar` **1778→1740**; new module 46 lines. `tsc` + `next build` clean; deployed `8a2d6f9` (HTTP 200). **Remaining P2-4:** more safe leaves (`PurgeButton`, `TrashedAgents`, the `Recent*` lists, `BrainSidebar`/`KnowledgeSidebarList`, the context/provider trio), then — runtime-gated — the list-dedup + touch-engine unification.
  - **Done — run 32 (second leaf: `TrashedAgents` + private `PurgeButton` → `components/sidebar/trashed-agents.tsx`):** moved the agents "recycle bin" out as one cohesive contiguous unit (source 1062–1170) — `TrashedAgents` (the collapsible trash list: restore + purge, rendered by `RecentAgents`) plus its module-private `PurgeButton` (the two-step arm→confirm delete, used only by `TrashedAgents`). Single external consumer (`RecentAgents`, which stays and now imports `TrashedAgents`) → clean move, no cross-file back-ref, no cycle (`useSidebar` is consumed only by the staying `SidebarMobileToggle`/`AppSidebar`, grep-confirmed). Recon defused two traps: `fmtEvery` (a lowercase cron-interval helper at 1172) belongs to `RecentCrons`, **not** this unit, so the block ends at `PurgeButton`'s close (1170); and the capitalised-ident scan's `ConfirmDeleteButton`/`Recycle`/`Mirrors`/`Two` were all **comment** noise (real-use grep excluding `//` → none). Real deps: react `{useState, useEffect}`, `trpc`, `cn`, `relTime`, lucide `{ChevronDown, Check, X, Trash2, RotateCcw}`. Byte-diff of both the function region (1065–1170) and the doc-comment (1062–1064) identical vs the new copy modulo the `export` on `TrashedAgents` (`PurgeButton` stays private). Import hygiene: `Check`/`ChevronDown`/`RotateCcw` were block-exclusive (per-icon count: total JSX use = in-block = 1 each) → **pruned** from `app-sidebar`'s lucide import; `Trash2`/`X` kept (each still used elsewhere — `Trash2` at a context-menu icon ~1566, `X` at another clear button ~1608). `app-sidebar` **1740→1631**; new module 122 lines. `tsc` + `next build` clean (an over-prune would be a hard `Cannot find name` tsc error, so the build proves the prune); deployed `2981a56` (HTTP 200). **Remaining P2-4:** more safe leaves (the `Recent*` lists, `BrainSidebar`/`KnowledgeSidebarList`, the header buttons, the context/provider trio), then — runtime-gated — the list-dedup + touch-engine unification.
  - **Done — run 33 (third leaf: header buttons `BrainButton`/`SettingsButton`/`NotificationsButton` → `components/sidebar/header-buttons.tsx`):** moved the three sidebar-header icon buttons out as one cohesive cluster (source 103–194) — `BrainButton` (hermit-crab → /brain), `SettingsButton` (→ Settings area), `NotificationsButton` (bell + unread badge → /notifications). All three rendered only by `AppSidebar`; none references a staying-local symbol (no `useSidebar`/NAV consts → no cycle). Deps: `usePathname`, `Link`, `cn`, lucide `{Settings, Bell}`, `{SETTINGS_HREFS, SETTINGS_ENTRY_HREF}` from settings-nav. Block byte-diff identical modulo the three `export`s. Two prune traps caught by the real-use-vs-noise guard: `Bell` looked block-exclusive but is also `NOTIF_FILTERS`'s `icon: Bell` (line 98, stays) → **kept**; `Settings` had 11 raw hits but only line 160 is the lucide icon (152/153 are `title=/aria-label=` strings, 688 a `>Settings<` text label, rest comments) → **pruned**. `SETTINGS_ENTRY_HREF` block-exclusive → pruned; `SETTINGS_HREFS` (~455) + `SETTINGS_TABS` (~849) kept. `app-sidebar` **1632→1539**; new module 105 lines. `tsc` + `next build` clean (over-prune = hard tsc error); deployed `a896862` (HTTP 200). **Remaining P2-4:** `Recent*` lists, `BrainSidebar`/`KnowledgeSidebarList`, the context/provider trio, then — runtime-gated — the list-dedup + touch-engine unification.
  - **Done — run 34 (fourth leaf: `BrainSidebar` + private `RecentBrainSessions` → `components/sidebar/brain-sidebar.tsx`):** moved the brain-mode sidebar out as one cohesive contiguous unit (source 104–214) — `BrainSidebar` (the /brain sidebar swap: "New 义脑 chat" + the brain's conversations) plus its module-private `RecentBrainSessions` (rendered only by `BrainSidebar`). Single external consumer (`AppSidebar`), no staying-local refs (block grep for `useSidebar`/`BRAIN_NAV`/sibling components → empty), so cycle-free — note `BrainSidebar` renders only `RecentBrainSessions`, while sibling `RecentDispatchSessions`/`KnowledgeSidebarList` are rendered by `AppSidebar` directly and stay. Deps: react `useState`, `useSearchParams`, `Link`, `trpc`, `cn`, `relTime`, lucide `SquarePen`. Block byte-diff identical modulo the `export` on `BrainSidebar` (`RecentBrainSessions` stays private). No prune this run: `SquarePen` looked block-exclusive by JSX count but is *also* `AppSidebar`'s new-chat entry `Icon: SquarePen` (line 392, a non-JSX value) → kept; everything else used throughout. `app-sidebar` **1539→1429** (git: −112/+1); new module 125 lines. `tsc` + `next build` clean; deployed `384cfc6` (HTTP 200). **Remaining P2-4:** `RecentDispatchSessions`/`KnowledgeSidebarList`, the `Recent*` lists, the context/provider trio, then — runtime-gated — the list-dedup + touch-engine unification.
  - **Done — run 35 (fifth leaf: `RecentDispatchSessions` co-located into `components/sidebar/brain-sidebar.tsx`):** the brain's dispatch-conversation list (rendered by `AppSidebar` on /brain/dispatch — a near-twin of `RecentBrainSessions`) is genuinely brain-mode, so rather than a new file it was **appended to the existing `brain-sidebar.tsx`** next to its siblings (first co-location, vs run 31–34's new-file moves). Source block 105–177 (doc + fn), single consumer `AppSidebar`, no staying-local refs (cycle-free). Deps ⊆ what `brain-sidebar.tsx` already imported **except `useMemo`** (that file had `useState`), so the only import change was `react` → `{ useState, useMemo }`; `useSearchParams`/`Link`/`trpc`/`cn`/`relTime` already there, and the block has no lucide icon. Fn (108–177) + doc (105–107) byte-diff identical vs the appended copy modulo the `export`. `AppSidebar` now imports `{ BrainSidebar, RecentDispatchSessions }`; module doc-comment updated. `app-sidebar` **1429→1355**; `brain-sidebar.tsx` 125→199. `tsc` + `next build` clean; deployed `8f4afaf` (HTTP 200). **Remaining P2-4:** `KnowledgeSidebarList`, the `Recent*` lists, the context/provider trio, then — runtime-gated — the list-dedup + touch-engine unification.
  - **Done — run 36 (sixth leaf: `KnowledgeSidebarList` → `components/sidebar/knowledge-sidebar-list.tsx`):** moved the knowledge-mode sidebar list (the /knowledge master list — KB rows linking to their editors — rendered by `AppSidebar` when `onKnowledge`) into its own new file (it's /knowledge-mode, not brain-mode, so co-locating into brain-sidebar.tsx would've been wrong). Block 105–165 (doc + fn), single consumer `AppSidebar`, no staying-local refs (cycle-free). Deps: `usePathname`, `Link`, `trpc` (the `knowledge.listBases` query), `cn`, lucide `BookOpen`. Fn (108–165) + doc (105–107) byte-diff identical modulo the `export`. No prune: `BookOpen` looked block-exclusive by `<BookOpen>` JSX but is *also* the `/knowledge` NAV entry's `icon: BookOpen` (line 76, a value use that stays) — the same value-use guard that kept `SquarePen`/`Bell`. `app-sidebar` **1355→1293**; new module 74 lines. `tsc` + `next build` clean; deployed `0bccd53` (HTTP 200). **Remaining P2-4:** the 4 still-inline `Recent*` lists (`RecentAgents`/`RecentCrons`+`fmtEvery`/`RecentSkills`/`RecentSessions`; `RecentBrainSessions`/`RecentDispatchSessions` already moved) + the context/provider trio, then — runtime-gated — the list-dedup + touch-engine unification.
  - **Done — run 37 (seventh leaf: context/provider trio → `components/sidebar/context.tsx`, barrel kept via re-export):** moved the sidebar-state API — `SidebarCtx` type + `Ctx` (module-private) + the three public exports `useSidebar`/`SidebarProvider`/`SidebarMobileToggle` (source 29–71) — into its own module. First extraction that touches the **barrel**: those three are imported from `@/components/app-sidebar` at ~18 external sites, so app-sidebar now does `import { … } from '@/components/sidebar/context'` (AppSidebar uses `useSidebar` internally) **plus** `export { useSidebar, SidebarProvider, SidebarMobileToggle }` — barrel unchanged for every consumer, `AppSidebar` stays a direct export. Cycle-free (trio uses only React + `cn` + `MenuIcon`, no app-sidebar-local refs). Block moved verbatim (the 3 fns already carry `export` in source → **no** transform; byte-diff 29–71 vs the copy identical with zero edits). Pruned the now-trio-only `createContext`/`useContext` (react) + `MenuIcon` (lucide) — each had exactly one real use, in the trio (the "outside=1" was just the import line). `app-sidebar` **1293→1255**; new module 55 lines. `tsc` + `next build` clean — the green build is the barrel proof: a broken re-export would fail every one of the ~18 consumers. deployed `e40db1a` (HTTP 200). **Remaining P2-4:** the four inline `Recent*` lists + the 4 NAV consts + `AppSidebar` (~490-line shell, stays), then — runtime-gated — the list-dedup + touch-engine unification.
  - **Done — run 38 (dead-code find: `RecentSkills` deleted — P3-2 overlap):** the P2-4 recon for "move a `Recent*` list" surfaced that `RecentSkills` (the /skills global-skill sidebar list, "Mirrors RecentCrons") is **dead code** — defined but never rendered: only `RecentSessions`/`RecentAgents`/`RecentCrons` are mounted (on /chat, /agents, /cron), and `onSkills` only feeds the New-skill CTA, not a recents list. Grep confirmed zero references across all of `src/` and it's non-exported, so deleting is behaviour-preserving by construction. Removed the block (793–879, 87 lines incl. doc + trailing blank); no import prune needed (its deps `Link`/`cn`/`trpc`/`useSearchParams` are all shared with the live lists). `app-sidebar` **1255→1168**. `tsc` + `next build` clean; deployed `402532a` (HTTP 200). **Remaining P2-4:** the 3 *live* `Recent*` lists (`RecentAgents`/`RecentCrons`+`fmtEvery`/`RecentSessions`) + the 4 NAV consts + `AppSidebar` (~490-line shell), then — runtime-gated — the list-dedup + touch-engine unification. (Also advances **P3-2** dead-code removal.)
  - **Done — run 39 (eighth leaf: `RecentCrons` + private `fmtEvery` → new shared `components/sidebar/recent-lists.tsx`):** moved the /cron sidebar list (`RecentCrons`, rendered by `AppSidebar` at line 538 — consumer verified live per run 38's lesson) plus its module-private `fmtEvery` interval-formatter (used only inside `RecentCrons` at 778) out as one contiguous block (source 686–791). Landed in a **new shared file** `recent-lists.tsx` — the intended home for the near-identical worker `Recent*` lists (`RecentAgents`/`RecentSessions` to follow) so the runtime-gated dedup has them all in one place. Cycle-free. Deps: `useState`, `useSearchParams`, `Link`, `trpc`, `cn`, `relTime`, `SidebarFindInput` (from the run-31 module — no cycle). Block byte-diff identical modulo the `export` on `RecentCrons` (`fmtEvery` stays private). No prune: `SidebarFindInput` (RecentAgents 597) + `relTime` (RecentAgents 644 / RecentSessions) still used by the staying lists. `app-sidebar` **1168→1061**; new module 123 lines. `tsc` + `next build` clean; deployed `7799cd0` (HTTP 200). **Remaining P2-4:** 2 live lists (`RecentAgents` + big `RecentSessions`) — co-locate into `recent-lists.tsx` — + 4 NAV consts + `AppSidebar` (~490-line shell), then — runtime-gated — the list-dedup + touch-engine unification.
  - **Done — run 40 (ninth leaf: `RecentAgents` co-located into `components/sidebar/recent-lists.tsx`):** moved the /agents sidebar list (`RecentAgents`, rendered by `AppSidebar` at line 538 — consumer verified live per run 38's guard) next to `RecentCrons` in the shared `recent-lists.tsx` (second co-location, continuing to gather the worker `Recent*` lists into one file for the runtime-gated dedup). Source block: doc 558–559 + fn 560–685. Cycle-free — the block references no `app-sidebar`-local symbol (grep for NAV consts / `useSidebar` / sibling `Recent*` / `AppSidebar` → empty), and the watch-list machinery (`sessionStatusView`/`isSessionUnread`/`useLiveWorking`/`usePins`/`togglePin`/`ContextMenu`/`useConfirm`/`useLongPress`/`Pin`) is `RecentSessions`'s, not here. Deps: `useCallback` (the hover-prefetch), `useState`, `useSearchParams`, `Link`, `trpc`, `cn`, `relTime`, `SidebarFindInput`, `TrashedAgents` — `recent-lists.tsx` was missing `useCallback` (it had only `useState`) and `TrashedAgents` (which `RecentAgents` renders), so those two imports were added; the rest were already present. Block byte-diff identical modulo the `export` on `RecentAgents`. **Prune** on the `app-sidebar` side: `TrashedAgents` (only consumer was `RecentAgents`) and `SidebarFindInput` (its last app-sidebar use was `RecentAgents` 598 — `RecentCrons` took its own copy in run 39) both fell to zero real uses → removed both imports; `relTime` (still `RecentSessions` 1029) and `useCallback` (still `RecentSessions` 702/777) stay. `app-sidebar` **1061→933**; `recent-lists.tsx` 123→252. `tsc` + `next build` clean (an over-prune would be a hard `Cannot find name` tsc error, so the green build proves the two prunes); deployed `3075dda` (HTTP 200). **Remaining P2-4:** the last inline list — the big `RecentSessions` (/chat) — to co-locate, + 4 NAV consts + `AppSidebar` (~490-line shell, stays), then — runtime-gated — the list-dedup + touch-engine unification.
  - **Done — run 41 (final leaf: `RecentSessions` co-located into `components/sidebar/recent-lists.tsx`; P2-4 safe-relocation sub-phase complete):** moved the /chat session list — the heaviest of the three worker lists (live-working dots, unread state, pins, a context menu, hide/hibernate/restart actions, long-press on touch) — out as the last inline block (source 556–931, no doc-comment, straight to EOF; no private sub-component after it). Consumer verified live (`{!collapsed && onChat && <RecentSessions />}`, line 535). Cycle-free — the block references no `app-sidebar`-local symbol, and because the move drags in 8 new source modules I explicitly checked none import back (`session-status`/`session-read`/`session-live`/`session-pins`/`use-long-press` + ui `select`/`context-menu`/`confirm-dialog` all clean). Block byte-diff identical modulo the `export`. `recent-lists.tsx` gained what it lacked: react `useMemo`/`useEffect`; 9 lucide icons (`Trash2`/`RotateCw`/`FoldVertical`/`X`/`Search`/`Pin`/`Eye`/`EyeOff`/`Moon`, all verified as `<Icon` JSX in the block); the 5 `Select` parts; `sessionStatusView`/`isSessionUnread`/`useLiveWorking`/`usePins`/`togglePin`/`useLongPress`/`ContextMenu`/`useConfirm`. **Big prune** on `app-sidebar` — 25 now-orphaned symbols (a per-symbol staying-region-vs-block scan drove it): react `useState`/`useMemo`/`useCallback` (state lives in the provider since run 37, so the shell uses only `useEffect`/`useRef` — direct-grep confirmed 0 staying uses), 8 lucide icons, `relTime`, the 5 `Select` parts, and the 8 session/ui hooks & components; `Moon` KEPT (BRAIN_NAV `icon: Moon`), `Link`/`cn`/`trpc`/`useSearchParams`/`useEffect`/`useRef` kept. The green tsc/build is the two-way proof (over-prune = hard `Cannot find name`; missing add breaks `recent-lists`). `app-sidebar` **931→554**; `recent-lists.tsx` 253→633. deployed `785910b` (HTTP 200). (Side find: `BarChart3` is a pre-existing dead lucide import, 0/0 — left as an out-of-scope P3-2 item.) **P2-4 → `[x]` scoped** (see the P2-4 line + run-41 changelog); loop advances to P2-5.
- [ ] **P2-5 · Per-session lock for the pollers** + coordinate the 6 module-level guard Sets into one owner. *Test:* gateway typecheck + restart; confirm no double-fire under overlapping ticks.
  - **Recon + design — run 42 (P2-5 decision run; no code change, mirrors run 30 for P2-3):** mapped the real guard landscape (correcting the review's "6 Sets"). The genuine in-flight guards are **3 session-keyed `Set<string>`** in `chat-runner.ts` — `settingUp` (110), `restartingSessions` (114), `hibernatingSessions` (189), all keyed by `sessionId` and coordinated around the `sessionStates` Map — plus **1 cron-keyed `Set`** `running` (cron-runner 48, keyed by cron id — a *different* key space) and **1 non-guard data Set** `pinnedUuids` (transcript-uuid pinning). Orthogonal: **4 whole-tick `let busy` booleans** (machine-requests / global-skills / agent-lifecycle / knowledge) guarding a whole poller's re-entry, not per-session. **Exception-safety audit:** the 3 session guards all release via `try/finally` (`restartOneSession` 129/156, `hibernateOneSession` 200/209, `settingUp` via `.finally()` 337 / try-finally 564–573) — no leak. Cron `running.delete` (278) sits just *after* its finally rather than inside it, but the whole body is fully guarded (catch 254 + guarded finally 257–261), so it's always reached in practice — a future-proofing note, not a bug. **The real gap** P2-5 targets is the *asymmetric* cross-checks: `chatTick` skips `settingUp`+`restartingSessions` (325/330/350/358), but `restartOneSession` guards only `restartingSessions` (ignores `hibernatingSessions`), `hibernateOneSession` only `hibernatingSessions` (ignores `restartingSessions`), and `reaperTick` consults none — so two lifecycle ops *can* touch one session at once (benign today: both are idempotent tear-downs and `killTmuxSession` no-ops on a dead pane; no incident on record). **Target design:** one `SessionLocks` owner (`tryAcquire(sessionId, op): boolean` / `release` / `withLock(id, op, fn)`, finally-released) replacing the 3 session Sets so every per-session lifecycle op mutually excludes uniformly; cron `running` stays separate (different key space); the 4 `let busy` stay. **Decision — defer live wiring:** the valuable form of this change *alters* race behavior (a unified lock makes ops mutually exclusive that today don't check each other), so its correctness ("no double-fire under overlapping ticks") is a probabilistic-race property a quick gateway restart + log-check cannot prove — and the blast radius is the whole fleet's live chat plumbing. Per the run-30 P2-3 precedent + the loop's "gateway concurrency = caution-first" rule, the live rewire is deferred until a race-stress harness / sustained runtime observation is available. P2-5 stays `[ ]` (nothing shipped); the loop advances to the actionable P3 hygiene items (verifiable via tsc + build + grep) in the meantime, and returns to wire `SessionLocks` (test-first: a pure module unit-tested on the P0-1 `tsx --test` harness, then incremental per-guard wiring) once runtime verification is back.

### P3 — hygiene (fold in opportunistically; each is independently shippable)

- [x] **P3-1 · Enums for stringly-typed state / status / role** (0 enums today; the class of bug behind the cron-status rework). *Test:* tsc catches every non-conforming literal. **✅ RESOLVED run 46 — recon-first per the prompt's bar (centralize only genuinely *scattered* unions; don't enum-ify single-definition ones for churn). Assessed every candidate: session `state` (`starting|working|idle`) = one zod def at the sync route → single-site, skip; `StatusView.key` = already centralized in `lib/session-status.ts`; message `role` = no union found; the per-router ack `z.enum(['done','error'])` / `['running','done','error']` = incidental structural coincidence across *independent* entity routers (knowledge/agents/skills/machines/fileStation) → merging = premature coupling, skip. The ONE domain with real scattered duplication: **cron status** — the literals (`ok|no_output|timeout|error` + legacy `fail` + `running`) were compared inline across **4 render sites** each re-implementing the same fail/error→red, timeout/no_output/running→amber, ok→green grouping (the documented "4-render-sites-must-change-together" hazard, see `hermit-ui-cron-status-semantics`). Centralized into new `lib/cron-status.ts`: `CRON_STATUS` const-object + derived `CronStatus` type + a `cronStatusTone()` classifier owning the grouping. Migrated all 4 (cron badge / agent-detail dot / sidebar dot / notifications) — each keeps its own tone→class map (visuals legitimately differ) but the raw status strings now live in exactly one place. Byte-behaviour-identical at every site (traced each branch incl. the divergent neutral fallbacks: cron/agent→zinc, sidebar→emerald). Gateway stays the one write-side producer with its own inline union (separate package). `tsc` + `next build` clean; deployed `32129dd`. See run-46 changelog.**
- [x] **P3-2 · Remove dead code** — `chat/page.tsx` `const inactive = false` + the `showRestartBar` render branch, `RestartBar` (extracted run 24 → now delete `components/chat/restart-bar.tsx` + its import + the branch), `streamingTailId`, `session-status.ts:37` `'idle'` union member. *Test:* tsc + build; grep confirms no references. **✅ RESOLVED run 43 — removed the RestartBar chain (deleted `components/chat/restart-bar.tsx` + its import + the always-false `showRestartBar` ternary branch + the orphaned `inactive`/`restartArmed`/`setRestartArmed`; `setRestartArmed` was a proven no-op in the live header button since `inactive=false` made `restartArmed` un-read), the legacy `'idle'` `StatusView.key` union member (no live caller compares to it — the other `'idle'` hits are a different type), and the unused `BarChart3` app-sidebar import (still live in settings-nav). `streamingTailId` was left — recon proved it LIVE (drives `isInFlight` + thinking-dots in page.tsx and the streaming-tail dot in message-timeline), not dead; the candidate was a mis-diagnosis. `tsc` + `next build` clean; grep confirms 0 residual refs. deployed `8be9833`. See run-43 changelog.**
- [x] **P3-3 · Consolidate the ~21 `x-asst-key` fetches + dual `getActiveKey` import** behind one client helper. *Test:* tsc + build; every call site still authenticates. **✅ RESOLVED run 44 — new `src/lib/asst-fetch.ts` `authedFetch(input, init?)` injects `x-asst-key` via `new Headers()` (reads the active key fresh from the keyring per call, never logged). Migrated the 5 identical `fetch(url, { headers: { 'x-asst-key': getActiveKey() } })` client sites (chat SSE stream, composer `/api/upload`, market skill upload+download, file-manager download) → each drops its `getActiveKey` import. Behaviour-preserving (same URL/method/body/signal/header). The review's "~21" over-counted — most were server-side `req.headers.get('x-asst-key')` reads + comments + two UI `placeholder="X-Asst-Key"` strings; the genuinely copy-pasted client fetch sites were 5. Left with their own paths (different shape, noted in the helper comment): the 2 XHR uploads (`setRequestHeader` among several; really a separate uploadXhr-dedup), the tRPC `httpBatchLink` `headers()` central injector (already single), the terminal WS subprotocol. tsc + build clean; 0 `x-asst-key` literals remain in the 5 files. deployed `3916318`. See run-44 changelog.**
- [x] **P3-4 · Error-handling hygiene** — audit `.catch(() => {})` swallows; make each either handle or log with intent. *Test:* tsc + build; no behavior change on the happy path. **✅ RESOLVED run 45 (dashboard scope) — audited all ~29 empty-catch swallows in `apps/dashboard/src`. The large majority are intentional idioms (value-fallback `null` sentinels on stat/fetchMachineByKey where the caller handles null; best-effort temp-file `rm` cleanup; best-effort localStorage / WS-send / xterm teardown) — left as-is. Two genuinely hid a failure → now logged (error-path only, happy path unchanged): file-station upload's swallowed `status='error'` DB write (silently stranded a transfer) → `console.error`; install-prompt's swallowed SW registration failure → `console.warn`. Added a clarifying comment to auth.ts's fire-and-forget lastSeen bump (its lastUsedAt sibling was already documented). Gateway-side swallows deferred to a separate pass (restart-gated; mostly best-effort teardown/telemetry). tsc + build clean. deployed `1fbb146`. See run-45 changelog.**
- [ ] **P3-5 · `mcp-stub.cjs` cleanup** — the 865-line untyped `.cjs` holding the machine key: split + typecheck where feasible without changing the no-dep/node-builtins-only constraint. *Lowest priority, handle-with-care.* *Test:* MCP tools still resolve from a fresh spawn; attach/ask smoke test.

---

## Changelog

_(the loop appends one entry per completed item)_

- **run 1 · P0-1** — test harness (`tsx --test`, wired at gateway + root `pnpm test`) + 19 pure-function tests (WORK_MARKER_RE / newestLineIsTurn / transcriptFresh / sessionTranscriptPath / tmuxPaneName / encodedProjectDir), all green; gateway typecheck clean. Behaviour-neutral (`export` only) — no deploy. commit `af5e182`.
- **run 2 · P0-2** — migration `20260715170000_chatsession_poller_indexes`: 4 partial indexes (`WHERE <col> IS NOT NULL`, keyed on machineId) for the cancel/restart/hibernate/dispatch pollers + composite `@@index([machineId, claudeSessionId])` for the interaction-sync fallback lookup (confirmed a real query predicate — the column is only *written* elsewhere, so this improves on the review's "just unindexed" note). `migrate deploy` → "All migrations successfully applied"; dashboard HTTP 200. Deployed `f9cf64b`. (EXPLAIN not run — psql off-limits — but each poller's WHERE exactly matches its partial predicate, so the planner uses them.)
- **run 3 · P1-1** — removed all 3 inline `hermit-${id…slice(-12)}` copies: gateway `chat-runner.ts` → `tmuxPaneName` (tmux-driver); dashboard `server.ts` + `terminal/page.tsx` → new pure, browser-safe `apps/dashboard/src/lib/pane-name.ts` (kept dashboard-local so the client bundle never pulls in the tmux/pty driver just to render a string). Behaviour-identical, covered by the P0-1 tmuxPaneName test. Gateway typecheck + 19 tests + dashboard typecheck + `next build` all green. Deployed: gateway pm2 restart (pid 27759→31230) + dashboard VPS HTTP 200. commit `e684669`. (The review's "5×" also counted tmux-driver's canonical impl + the unit test; 3 real inline copies. The Node/browser split leaves 2 canonical sources — the P1-3 shared-contract module could later unify them.)
- **run 4 · P1-2** — session-snapshot's local `transcriptPath()` now delegates the `~/.claude/projects/<enc>/<uuid>.jsonl` derivation to pane.ts's `sessionTranscriptPath` (the single source), keeping only its own `fs.existsSync` check on top (a pruned/absent transcript stays null). Dropped the now-unused `encodedProjectDir` import. Behaviour-identical; gateway typecheck + 19 tests green; runtime-verified (gateway restart pid 31230→9689, `[session-snapshots]` ticks back to steady `ok` ~600ms after the restart warm-up). commit `06396ea`.
- **run 5 · P1-3** — new `apps/gateway/src/claude-code.ts` centralizes the transcript vocabulary (`CcEvent`/`CcBlock` constants + `NON_TURN_EVENT_TYPES`) + the parsing predicates: killed **3 identical copies of `extractText`** (chat-runner/cron-runner/session-snapshot — cron's `.trim()` moved to its call site so it stays identical) + the hand-inlined `hasToolResult`/`hasToolUse` + the `'assistant'`/`'user'`/`'tool_use'`/`'tool_result'`/`'bridge-session'` literals scattered across 4 hot files. New `claude-code.test.ts` (10 tests). Gateway typecheck + 29 tests green; runtime-verified (gateway restart → `[session-snapshots]` + `[chat-tick]` back to steady `ok` after the reconnect-flood warm-up, dashboard 200 throughout). commits `bf08bc1` + `72badb6`. (Left `'system'`/`'compact_boundary'` as specialized single-use; the tmux-side contract — pane names, resume prompts — stays in @hermit-ui/tmux-driver.)
- **run 46 · P3-1 (centralize cron status → tone grouping)** — recon-first per the item's bar: centralize a stringly-typed union only when it's *genuinely scattered* (≥2 independent redefinitions), never enum-ify a single-definition union for churn. Surveyed the candidates: session `state` (`starting|working|idle`) is defined once (the `session-snapshot` sync-route `z.enum`) → single-site, no value; `StatusView.key` is already the centralized derived union in `lib/session-status.ts`; message `role` has no shared union at all; the per-router ack enums `z.enum(['done','error'])` (knowledge/agents/skills) + `z.enum(['running','done','error'])` (machines/fileStation) are an *incidental* structural coincidence across independent entity routers — folding them into one shared enum would couple five unrelated mutations, so left apart (documented, not forced). The one domain with real scattered duplication is **cron status**: the run-status literals (`ok`/`no_output`/`timeout`/`error` + transient `running` + legacy `fail`) were compared inline across **4 render sites** — the `/cron` `CronStatusBadge`, the agent-detail dot, the sidebar recents dot, the notifications inbox — each independently re-deriving the same grouping (fail/error → red, timeout/no_output/running → amber, ok → green). Exactly the "4-render-sites-must-change-together" hazard recorded in memory `hermit-ui-cron-status-semantics`. New `apps/dashboard/src/lib/cron-status.ts` is the single read-side source: `CRON_STATUS` (`as const` object) + derived `CronStatus` type + a `cronStatusTone(status): 'ok'|'bad'|'inconclusive'|'neutral'` classifier owning the status→tone grouping in ONE switch. Each site keeps its own *tone→class* map (a badge class-triple vs a dot bg — the visuals legitimately differ, and the neutral fallback differs too: cron/agent-detail → zinc, sidebar → emerald), but the raw status strings now appear in exactly one place. Behaviour byte-identical at all four (each branch traced, incl. the `running`-pulse which stays a literal `=== CRON_STATUS.running` check — only true `running` pulses; routing it through the inconclusive tone would wrongly pulse timeout/no_output). The gateway stays the one *write-side* producer with its own inline `'ok'|'no_output'|'timeout'|'error'` union (separate package; the dashboard never imports gateway internals — noted in the module header). +81/−38 across 5 files (1 new module + 4 sites). `tsc --noEmit` + `next build` clean. deployed `32129dd` (HTTP 200, matching commit). commit `32129dd`. **P3-1 → `[x]`.** Remaining backlog is **only P2-5 (per-session poller lock — gateway concurrency, deferred: needs live overlapping-tick observation to prove no double-fire, fleet blast-radius) and P3-5 (mcp-stub split — gateway, 865-line untyped `.cjs` holding the machine key, handle-with-care, needs fresh-spawn MCP-resolution verification)** — both gateway-side + runtime-gated, not safely verifiable from this dashboard-only loop. The safely-advanceable dashboard items are now all `[x]`; honest status handed to sway (see reply).
- **run 45 · P3-4 (error-handling hygiene — dashboard `.catch` swallow audit)** — audited every empty-catch swallow in `apps/dashboard/src` (~14 `.catch(() => {}/null/undefined)` + ~15 single-line `catch {}` blocks). The audit's honest finding: the large majority are *intentional idioms*, not bugs — (1) `.catch(() => null)` **value-fallbacks** where `null` is a handled sentinel (`stat(...).catch(() => null)` for existence checks in the uploads/upload routes; `fetchMachineByKey(k).catch(() => null)` for key validation in auth-gate / add-machine / keyring); (2) best-effort **temp-file cleanup** (`rm(tmp, { force: true }).catch(() => {})` / `dropTemp`); (3) best-effort **localStorage** persistence (`try { localStorage.setItem(...) } catch {}` — throws in private mode / SSR / quota), **WS send**, and **xterm teardown** (`try { sock.close() } catch {}`, `try { term.dispose() } catch {}` on a possibly-dead socket, all in terminal-view). None of those hide a failure a user/dev needs — logging them would spam the happy path — so they stay. **Two genuinely swallowed a real failure and now log** (error-path only, zero happy-path change): `file-station/upload/route.ts` — the `prisma.fileTransfer.update({ status: 'error' })` that marks a transfer errored after a stash-rename failure was itself `.catch(() => {})`, so a failed mark silently stranded the row in a non-error state → now `.catch((err) => console.error('[file-station] failed to mark transfer errored after stash failure:', err))`; `install-prompt.tsx` — `navigator.serviceWorker.register('/sw.js').catch(() => {})` hid SW-registration failures that explain a missing install prompt → now `console.warn('[pwa] service worker registration failed:', e)`. Also added a clarifying comment to `auth.ts`'s debounced fire-and-forget `lastSeen` bump (its share-link `lastUsedAt` sibling was already documented; now the pair is symmetric). No secret/key is ever logged (the logged failures are DB/SW errors, not credentials). +5/−2 across 3 files. `tsc --noEmit` + `next build` clean. deployed `1fbb146` (HTTP 200, matching commit). commit `1fbb146`. **P3-4 → `[x]` (dashboard scope)** — the gateway's `.catch(() => {})` swallows (a larger set, mostly best-effort teardown/telemetry) are a deliberate separate follow-up: they need a gateway pm2 restart to verify and carry fleet blast-radius, so they're out of scope for this dashboard-verifiable pass. Loop advances to P3-1 (enums) or P3-5 (mcp-stub) next; P2-5 still deferred to runtime.
- **run 44 · P3-3 (consolidate client `x-asst-key` fetches → `authedFetch`)** — the review flagged "~21 `x-asst-key` fetches + a dual `getActiveKey` import"; recon found the raw count inflated (server-side `req.headers.get('x-asst-key')` reads in the API routes + trpc + auth, doc comments, and two UI `placeholder="X-Asst-Key"` strings make up most of it). The genuinely copy-pasted **client** injection is 5 identical `fetch(url, { headers: { 'x-asst-key': getActiveKey() } })` sites: the chat SSE stream (`chat/page.tsx`, carries a `signal`), the composer `/api/upload` (`POST` + `body: fd`), the market skill upload + download (`upload-skill-dialog` / `market-skill-detail`), and the file-manager download (`file-explorer/core`). New `src/lib/asst-fetch.ts` exposes `authedFetch(input, init?)` — builds `new Headers(init.headers)` (robust to any caller-supplied header form) then `.set('x-asst-key', getActiveKey())`, key read fresh per call from the keyring's active entry, never logged. Migrated all 5; each drops its own `getActiveKey` import (getActiveKey was used only at the fetch site in every one — grep-confirmed), collapsing part of the dual-import sprawl. Behaviour-preserving by construction: same URLs, same method/body/signal, same single `x-asst-key` header. **Deliberately left alone** (different shape, noted in the helper's header comment): the 2 XHR uploads (`file-station`/`agent-files`, which `setRequestHeader('x-asst-key', …)` alongside `x-file-name`/`x-file-path`/`x-file-unzip` — a fetch wrapper doesn't fit; those two are really a separate uploadXhr-dedup opportunity), the tRPC `httpBatchLink` `headers()` link (already the single central injector for every tRPC call), and the terminal WS which carries the key as a `hermit-key.<token>` subprotocol. `getActiveKey` now imports only where it belongs: the new helper + the tRPC link/re-export in `providers.tsx` + the WS + the 2 XHR + its keyring definition. +28/−13 across 6 files. `tsc --noEmit` + `next build` clean; grep confirms 0 `x-asst-key` literals remain in the 5 migrated files. deployed `3916318` (HTTP 200, matching commit). commit `3916318`. **P3-3 → `[x]`.** Loop advances to P3-4 (error-handling hygiene — audit `.catch(() => {})` swallows) next; P3-1 (enums) and P3-5 (mcp-stub) remain; P2-5 still deferred to runtime.
- **run 43 · P3-2 (dead code removal — RestartBar chain + legacy `'idle'` union + `BarChart3`)** — first P3 hygiene item after P2-5 was deferred; all dashboard-side, fully verified by tsc + build + grep (behaviour-preserving by construction — removing genuinely-dead code meets the same bar as a byte-identical move, safe with Playwright down). **RestartBar chain:** `chat/page.tsx` had `const inactive = false` (hardcoded) → `showRestartBar = inactive && !restartArmed` always false → the `{showRestartBar ? <RestartBar/> : <composer/>}` ternary never took the RestartBar branch; the code self-documented it ("RestartBar + restartArmed are vestigial now; safe to delete"). Deleted the whole file `components/chat/restart-bar.tsx` (33 lines) + its import + collapsed the ternary to just the composer branch + removed the orphaned `restartArmed`/`setRestartArmed` state. That state needed care: `setRestartArmed` was still *called* by the live header restart button, but `restartArmed` was *read* only by the now-removed `showRestartBar`, and since `inactive=false` forced `showRestartBar` false regardless, the call was a **proven no-op** — so stripping it from the header is behaviour-preserving. **`'idle'` union:** dropped the legacy `'idle'` member from `StatusView.key` in `session-status.ts` (its own comment said "never returned anymore … drop once every caller is updated"); a full-src scan confirmed **no live caller compares `StatusView.key` to `'idle'`** (the other `'idle'` hits are a *different* type — the session-snapshot `state` enum and `format.ts`'s `running/stuck/idle` switch — neither is `StatusView.key`), so tsc stays green. **`BarChart3`:** removed the unused lucide import from `app-sidebar.tsx` (flagged 0/0 in run 41; still imported+used in `settings-nav.ts`, left untouched). **`streamingTailId` NOT removed** — the backlog listed it as dead, but recon proved it **live**: it drives `isInFlight` + the thinking-dots gate in `page.tsx` and threads into `message-timeline.tsx` as `streamingTail` → the streaming-tail dot in `MessageRow` (used at 106/169/235/251). The backlog candidate conflated it with the *typewriter-decision* note ("don't use post-render streamingTailId") — a different concern; the symbol itself is live. Left as-is + recorded (like run 2 / run 42 correcting review notes). 3 insertions / 58 deletions total. `tsc --noEmit` + `next build` clean; grep confirms 0 residual references to every removed symbol. deployed `8be9833` (HTTP 200, matching commit). commit `8be9833`. **P3-2 → `[x]`.** Loop advances to P3-3 (consolidate the ~21 `x-asst-key` fetches behind one client helper — dashboard-side, tsc+build verifiable). (P2-5 still deferred to runtime.)
- **run 42 · P2-5 (recon + design decision run; no code change)** — first gateway-side item after the P2-4 dashboard extractions, so recon-first per the loop's "gateway concurrency = caution" rule. Mapped the real guard landscape (correcting the review's "6 module-level guard Sets"): the genuine in-flight guards are **3 session-keyed Sets** in `chat-runner.ts` (`settingUp`/`restartingSessions`/`hibernatingSessions`, all `sessionId`-keyed, coordinated around the `sessionStates` Map), **1 cron-keyed Set** (`running` in cron-runner, keyed by cron id — a different key space), and **1 non-guard data Set** (`pinnedUuids`, transcript pinning); orthogonal to all of them are **4 whole-tick `let busy` booleans** (machine-requests / global-skills / agent-lifecycle / knowledge) guarding whole-poller re-entry. Exception-safety audit: all 3 session guards release via `try/finally` (verified line-by-line, including `restartOneSession` 129/156 and `hibernateOneSession` 200/209) — no leak; cron `running.delete` (278) sits just after (not inside) its finally, but the body is fully guarded so it's always reached in practice (future-proofing note, not a bug). The real thing P2-5 targets is the **asymmetric cross-checks** — `chatTick` checks `settingUp`+`restartingSessions`, but `restartOneSession` ignores `hibernatingSessions`, `hibernateOneSession` ignores `restartingSessions`, and `reaperTick` consults none — so two lifecycle ops can touch one session at once (benign today: both are idempotent tear-downs, no incident on record). **Target:** one `SessionLocks` owner (`tryAcquire`/`release`/`withLock`, finally-released) unifying the 3 session guards; cron `running` stays separate; the `let busy` booleans stay. **Decision (mirrors run 30 for P2-3):** the valuable form *changes* race behavior (unified mutual exclusion where today some ops don't check each other), so "no double-fire under overlapping ticks" is a probabilistic-race property a quick restart+log-check can't prove, and the blast radius is the whole fleet's live chat plumbing — so the live rewire is deferred (test-first when a race-stress harness / runtime observation returns: a pure `SessionLocks` module unit-tested on the `tsx --test` harness, then incremental per-guard wiring with gateway typecheck + restart + observation). No code/deploy this run — pure recon, claims verified by direct code inspection. P2-5 stays `[ ]`; the loop works the actionable P3 hygiene items next (P3-2 dead code is ready — run 38 already deleted `RecentSkills`, run 41 flagged the dead `BarChart3` lucide import). docs-only commit.
- **run 41 · P2-4 (final leaf: `RecentSessions` co-located into `recent-lists.tsx`; P2-4 scoped-closed)** — moved the last inline sidebar list, the big `/chat` session list, out of `app-sidebar.tsx` to join `RecentCrons` + `RecentAgents` in the shared `recent-lists.tsx` — so all three near-identical worker `Recent*` lists now live in one file. Consumer-survival guard first: `{!collapsed && onChat && <RecentSessions />}` (line 535) renders it. `RecentSessions` is the heaviest list (live-working dots, unread state, pins, a context menu, hide/hibernate/restart actions, long-press on touch); its source runs 556–931 straight to EOF with no doc-comment and no trailing private sub-component. Recon confirmed the block references no `app-sidebar`-local symbol (cycle-free), and — because this move drags in 8 new source modules — I explicitly checked that none of `session-status`/`session-read`/`session-live`/`session-pins`/`use-long-press` or ui `select`/`context-menu`/`confirm-dialog` import back (all clean). The block byte-diffs identical against the appended copy modulo the single `export`. `recent-lists.tsx` gained everything it was missing: react `useMemo`/`useEffect`, a lucide line (`Trash2`/`RotateCw`/`FoldVertical`/`X`/`Search`/`Pin`/`Eye`/`EyeOff`/`Moon` — all 9 verified as `<Icon` JSX in the block), the 5 `Select` parts, and `sessionStatusView`/`isSessionUnread`/`useLiveWorking`/`usePins`/`togglePin`/`useLongPress`/`ContextMenu`/`useConfirm`. A per-symbol staying-region(32–554)-vs-block(556–931) use-count scan then drove a **25-symbol prune** from `app-sidebar`: react `useState`/`useMemo`/`useCallback` (the shell's state moved into the provider in run 37, so it now uses only `useEffect`/`useRef` — direct-grep confirmed 0 staying uses), 8 lucide icons, `relTime`, the 5 `Select` parts, and the 8 session/ui hooks & components; `Moon` was KEPT via the value-use guard (BRAIN_NAV `icon: Moon`), and `Link`/`cn`/`trpc`/`useSearchParams`/`useEffect`/`useRef` stay (still used by the shell). The green `tsc --noEmit` + `next build` is the two-way proof — an over-prune is a hard `Cannot find name`, a missing add breaks `recent-lists`. `app-sidebar.tsx` **931→554**; `recent-lists.tsx` 253→633. deployed `785910b` (HTTP 200, matching commit). commit `785910b`. **P2-4 → `[x]` (scoped):** every leaf and all three worker lists are now extracted (runs 31–41: nine relocations + one dead-code delete into seven `components/sidebar/` modules); `app-sidebar.tsx` is decomposed 1778→554 (−69%) to just imports + the barrel re-export + 4 NAV consts + the ~490-line AppSidebar shell. The remaining "fold the near-identical lists into one parameterised unit + extract the touch engine" is a true structural refactor (component structure / state ownership / prop interfaces) needing runtime parity verification that's been unavailable all session (Playwright dead) — consciously DEFERRED, mirroring the P2-3 run-30 scoped-done call, and now set up to happen inside the single `recent-lists.tsx` when a runtime harness returns. (Side find: `BarChart3` is a pre-existing dead lucide import, 0 uses — left as an out-of-scope P3-2 item.) Loop advances to **P2-5** next (gateway per-session lock — testable via gateway typecheck + restart, no Playwright needed).
- **run 40 · P2-4 (ninth leaf: `RecentAgents` co-located into `components/sidebar/recent-lists.tsx`)** — continued the safe byte-identical moves by lifting the /agents sidebar list out of `app-sidebar.tsx` to sit beside `RecentCrons` in the shared `recent-lists.tsx` (the second co-location; run 39 opened the file). Consumer-survival guard first (run 38's lesson): `RecentAgents` is genuinely rendered — `{!collapsed && onAgents && <RecentAgents />}` at line 538 — a real move, not dead code. Recon of the source block (doc 558–559 + fn 560–685) confirmed it references no `app-sidebar`-local symbol (cycle-free), and that the heavy `RecentSessions`-only machinery the prompt flagged to watch for — `sessionStatusView`/`isSessionUnread`/`useLiveWorking`/`usePins`/`togglePin`/`ContextMenu`/`useConfirm`/`useLongPress`/`Pin` — is absent here, making `RecentAgents` the simpler of the two remaining lists. Its real deps are `useCallback` (the hover→`utils.agents.byName.prefetch` intent-prefetch), `useState`, `useSearchParams`, `Link`, `trpc`, `cn`, `relTime`, `SidebarFindInput`, and `TrashedAgents` (which it renders). `recent-lists.tsx` already had all but two — `useCallback` (the file had only `useState`) and `TrashedAgents` — so exactly those two imports were added there; everything else was already present, no duplicate-import churn. The block byte-diffs identical against the appended copy modulo the single `export` added to `RecentAgents`. This move finally lets **two** app-sidebar imports be pruned: `TrashedAgents` (only consumer was `RecentAgents`) and `SidebarFindInput` (whose last app-sidebar use was `RecentAgents` 598 — `RecentCrons` took its own copy into `recent-lists.tsx` in run 39) both drop to zero real uses in `app-sidebar`; `relTime` (still used by `RecentSessions` 1029) and `useCallback` (still `RecentSessions` 702/777) stay. `app-sidebar.tsx` **1061→933**; `recent-lists.tsx` 123→252. `tsc --noEmit` + `next build` clean — an over-prune is a hard `Cannot find name` tsc error, so the green build is the proof the two prunes are correct. deployed `3075dda` (HTTP 200, matching commit). Playwright's dead-CDP wall keeps this a byte-identical-relocation-by-construction run (block byte-diff + tsc + build); the runtime-gated dedup stays deferred. commit `3075dda`. **P2-4 stays `[ ]`** — `app-sidebar.tsx` now holds the four NAV consts, `AppSidebar` (~490-line shell, stays), and just **one** inline list left: the large `RecentSessions` (/chat). Next: co-locate `RecentSessions` into `recent-lists.tsx` (the last inline list), then the runtime-gated dedup + touch-engine unification. (Cumulative P2-4: `app-sidebar.tsx` 1778→933, −845; nine leaves + one dead-code removal into seven `components/sidebar/` modules.)
- **run 39 · P2-4 (eighth leaf: `RecentCrons` + `fmtEvery` → new shared `components/sidebar/recent-lists.tsx`)** — resumed the safe byte-identical moves (run 38 was the dead-`RecentSkills` detour) by lifting the /cron sidebar list out of `app-sidebar.tsx`. First applied run 38's consumer-survival guard: `RecentCrons` is genuinely rendered — `{!collapsed && onCron && <RecentCrons />}` at line 538 — so it's a real move, not another dead-code find. It travels with its module-private helper `fmtEvery` (the "every Nh/Nm/Ns" cron-interval formatter), which a `\bfmtEvery\b` scan showed is used at exactly one call site, inside `RecentCrons` (line 778) — so the two move together as one contiguous source block (686–791: `fmtEvery`, then `RecentCrons` with its doc-comment) and `fmtEvery` stays unexported in the new file. Rather than a per-leaf file, this one opens a **new shared `recent-lists.tsx`** — deliberately the future home for the near-identical worker `Recent*` lists (`RecentCrons` now; `RecentAgents` and the big `RecentSessions` to co-locate next), so that when the runtime-gated dedup finally folds them into one parameterised `<RecentList>` it operates within a single file instead of gathering them back up. Recon confirmed the block references no `app-sidebar`-local symbol (cycle-free); its deps are `useState`, `useSearchParams` (next/navigation), `Link`, `trpc`, `cn`, `relTime`, and `SidebarFindInput` — the last imported from the run-31 `sidebar-find-input` module, which doesn't import back, so no cycle. The block byte-diffs identical against the new copy modulo the single `export` added to `RecentCrons`. No import prune on the `app-sidebar` side: `SidebarFindInput` is still used by `RecentAgents` (line 597) and `relTime` by both `RecentAgents` (644) and `RecentSessions`, so both stay. `app-sidebar.tsx` **1168→1061**; new `recent-lists.tsx` is 123 lines. `tsc --noEmit` + `next build` clean; deployed `7799cd0` (HTTP 200, matching commit). Playwright's dead-CDP wall keeps this a byte-identical-relocation-by-construction run (block byte-diff + tsc + build); the runtime-gated dedup stays deferred. commit `7799cd0`. **P2-4 stays `[ ]`** — `app-sidebar.tsx` now holds the four NAV consts, `AppSidebar` (~490-line shell, stays), and just **two** inline lists left: `RecentAgents` (which renders the moved `TrashedAgents`) and the large `RecentSessions` (/chat). Next: co-locate `RecentAgents` into `recent-lists.tsx`, then `RecentSessions`, then the runtime-gated dedup + touch-engine unification. (Cumulative P2-4: `app-sidebar.tsx` 1778→1061, −717; eight leaves + one dead-code removal into seven `components/sidebar/` modules.)
- **run 38 · P2-4 recon → P3-2 (dead code: `RecentSkills` deleted)** — this run's P2-4 target was "move a `Recent*` list", but the recon turned up something better to do: `RecentSkills` — the "Global-skill list shown in the sidebar on /skills … Mirrors RecentCrons" component — is **dead code**. Only three of the four `Recent*` lists are ever mounted (`RecentSessions` on /chat, `RecentAgents` on /agents, `RecentCrons` on /cron, at lines 536–538); there is no `<RecentSkills />` anywhere, and the `/skills` route's `onSkills` flag only drives the "New skill" CTA, never a recents list (the skills list lives in the page body, not the sidebar). A whole-`src/` grep confirmed `RecentSkills` appears at exactly one place — its own definition — and it is not exported, so nothing outside the file could reach it either. Deleting it is therefore behaviour-preserving *by construction* (no consumer to change behaviour for), the same standard the byte-identical moves meet, so it's safe under the loop's guardrails even with Playwright dead. Removed the 87-line block (the two-line doc-comment + the function + its trailing blank, source 793–879); no import needed pruning because everything `RecentSkills` used (`Link`, `cn`, `trpc`, `useSearchParams`) is also used by the live lists that stay. `app-sidebar.tsx` **1255→1168** (git: 87 deletions, 0 insertions). `tsc --noEmit` + `next build` clean; deployed `402532a` (HTTP 200, matching commit). This is really a **P3-2** (dead-code removal) win surfaced by the P2-4 recon — logged here to keep the running narrative, and it also shrinks the god-file, so it serves both. commit `402532a`. **P2-4 stays `[ ]`** — the four inline sidebar lists are now three (all live): `RecentAgents` (renders the moved `TrashedAgents`), `RecentCrons` (+ its `fmtEvery` helper), and the big `RecentSessions` (/chat). Next: move one of those live lists to `components/sidebar/`, then the runtime-gated 6-list dedup + touch-engine unification. (Cumulative P2-4 recon+moves: `app-sidebar.tsx` 1778→1168, −610.)
- **run 37 · P2-4 (seventh leaf: context/provider trio → `components/sidebar/context.tsx`, barrel via re-export)** — continued the safe byte-identical phase with the first extraction that touches the public **barrel**. Moved the sidebar-state API out of `app-sidebar.tsx` into a new `components/sidebar/context.tsx`: the `SidebarCtx` type and `Ctx = createContext(...)` (both module-private), plus the three public members `useSidebar` (the `useContext` hook + its "must be inside SidebarProvider" guard), `SidebarProvider` (collapse/mobile state + the localStorage persistence + the memoized value), and `SidebarMobileToggle` (the hamburger button). These three are imported from `@/components/app-sidebar` at ~18 sites across the app (page headers, `auth-gate`, `scoped-sidebar`, `settings-tabs`, `new-chat-pane`), so the barrel MUST keep exporting them — done with an `export { useSidebar, SidebarProvider, SidebarMobileToggle }` re-export beside a matching `import` (AppSidebar consumes `useSidebar` itself), leaving `AppSidebar` as the file's own direct export; the four barrel names are unchanged for every consumer. The move is cycle-free: the trio references only React hooks, `cn`, and the `MenuIcon` lucide icon — no `app-sidebar`-local symbol — so `context.tsx` never imports back. Because all three functions already carry `export function` in the source, the block (29–71) was relocated *verbatim* — the byte-diff against the new copy is identical with **no** transform at all (unlike the leaf moves, which added an `export`). Import hygiene on the `app-sidebar` side: `createContext`, `useContext`, and `MenuIcon` were each used in exactly one place — inside the trio — so all three were pruned (the naive per-symbol "outside = 1" counts were just their own import-line occurrence, confirmed by a real-use grep excluding line 3 / line 7); the other react hooks (`useState`/`useEffect`/`useCallback`/`useMemo`/`useRef`) all have real uses in `AppSidebar` and stayed. `app-sidebar.tsx` **1293→1255**; new `context.tsx` is 55 lines. `tsc --noEmit` + `next build` clean, and this run the green build is a direct proof of the barrel: a mistyped or missing re-export would break every one of the ~18 `import { … } from '@/components/app-sidebar'` sites with a hard "no exported member" error, not a warning. deployed `e40db1a` (HTTP 200, matching commit) — the production app renders, so the re-export resolves at runtime too. Playwright's dead-CDP wall keeps this a byte-identical-relocation-by-construction run (the verbatim byte-diff + tsc + build + the barrel-consumer coverage); the runtime-gated list-dedup stays deferred. commit `e40db1a`. **P2-4 stays `[ ]`** — `app-sidebar.tsx` now holds only the four NAV consts, `AppSidebar` itself (~490-line orchestrating shell, stays by design), and the four still-inline `Recent*` lists (`RecentAgents`/`RecentCrons`+`fmtEvery`/`RecentSkills`/`RecentSessions`). Next: the `Recent*` lists (each a safe leaf; they are the deferred dedup's inputs, so moving them to `components/sidebar/` also stages that refactor), then the runtime-gated 6-list dedup + touch-engine unification. (Cumulative P2-4: `app-sidebar.tsx` 1778→1255, −523; seven leaves into six `components/sidebar/` modules.)
- **run 36 · P2-4 (sixth leaf: `KnowledgeSidebarList` → `components/sidebar/`)** — continued the safe byte-identical phase by moving the knowledge-mode sidebar list into its own new `components/sidebar/knowledge-sidebar-list.tsx`. `KnowledgeSidebarList` is the /knowledge master list (the master-detail layout's left pane, where /knowledge/[slug] is the detail — each row a knowledge base linking to its editor), rendered by `AppSidebar` only when `onKnowledge`. Because it's a *knowledge*-mode piece (not brain-mode), it gets its own file rather than co-locating into run 35's `brain-sidebar.tsx` — co-location is for genuinely-related pieces, not just any leaf. Source block 105–165 (doc-comment 105–107 + fn 108–165), single external consumer `AppSidebar`, recon confirmed no staying-local reference (no `useSidebar`/NAV consts/sibling components) so cycle-free. Deps: `usePathname` (next/navigation), `Link` (next/link), `trpc` (the `knowledge.listBases.useQuery`), `cn`, and lucide `BookOpen`. The function (108–165) and its doc-comment (105–107) each byte-diff identical against the new copy modulo the added `export`. No import prune this run, again on the value-use guard that has now fired three runs running (`Settings` run 33, `SquarePen` run 34, and here): `BookOpen` appears in-block once as the `<BookOpen>` row icon, but a JSX-only count would wrongly call it block-exclusive — it's *also* the `/knowledge` entry's `icon: BookOpen` in the staying `NAV` array (line 76), a non-JSX value use, so it's kept; `usePathname`/`Link`/`trpc`/`cn` are used across the file. `app-sidebar.tsx` **1355→1293**; new `knowledge-sidebar-list.tsx` is 74 lines. `tsc --noEmit` + `next build` clean; deployed `0bccd53` (HTTP 200, matching commit). Playwright's dead-CDP wall keeps this a byte-identical-relocation-by-construction run (the two byte-diffs + tsc + build); the runtime-gated list-dedup stays deferred. commit `0bccd53`. **P2-4 stays `[ ]`** — the remaining decls in `app-sidebar.tsx` are now: the context/provider trio (`useSidebar`/`SidebarProvider`/`SidebarMobileToggle` + `SidebarCtx`/`Ctx`), the four NAV consts, `AppSidebar` itself (~490 lines, the orchestrating shell — stays), and the four still-inline `Recent*` lists (`RecentAgents` [imports the moved `TrashedAgents`], `RecentCrons` + its `fmtEvery` helper, `RecentSkills`, `RecentSessions`). Next: a `Recent*` list or the context-provider trio (the latter needs an `app-sidebar` re-export to keep the 18-site barrel intact), then the runtime-gated 6-list dedup + touch-engine unification. (Cumulative P2-4: `app-sidebar.tsx` 1778→1293, −485; six leaves into five `components/sidebar/` modules.)
- **run 35 · P2-4 (fifth leaf: `RecentDispatchSessions` → co-located in `brain-sidebar.tsx`)** — continued the safe byte-identical phase, but with a first for P2-4: instead of a new file, the leaf was **co-located into the module extracted last run**. `RecentDispatchSessions` (the brain's `origin:'dispatch'` conversation list, rendered by `AppSidebar` when on /brain/dispatch — structurally a near-twin of run 34's `RecentBrainSessions`) is genuinely a brain-mode sidebar piece, so appending it to `components/sidebar/brain-sidebar.tsx` next to `BrainSidebar`/`RecentBrainSessions` groups the brain-mode chrome in one cohesive module rather than scattering it across per-leaf files. Source region 105–177 (doc-comment 105–107 + fn 108–177), single external consumer `AppSidebar`, and recon confirmed no staying-local reference (no `useSidebar`/NAV consts/sibling components) so the move is cycle-free. Its dependency set — `useMemo` (react), `useSearchParams` (next/navigation), `Link`, `trpc`, `cn`, `relTime`, and only `<Link>` in JSX (no lucide icon) — was already a subset of what `brain-sidebar.tsx` imported **except `useMemo`** (the file had `useState` for `BrainSidebar`/`RecentBrainSessions`), so the sole import change on the receiving side was widening its react import to `{ useState, useMemo }`; everything else was already present, so no duplicate-import churn. The append was verified two ways: the function region (source 108–177) and its doc-comment (105–107) each byte-diff identical against the appended copy, modulo the `export` added to `RecentDispatchSessions`. On the `app-sidebar` side the def was `sed`-deleted (105–178, incl. the trailing blank — the seam cleanly rejoins `NOTIF_FILTERS` to the `KnowledgeSidebarList` doc-comment that follows) and its import line widened to `{ BrainSidebar, RecentDispatchSessions }`; the receiving module's doc-comment was updated to note both are rendered by `AppSidebar`. `app-sidebar.tsx` **1429→1355**; `brain-sidebar.tsx` **125→199**. `tsc --noEmit` + `next build` clean (a *missing* `useMemo` would be a hard error, so the green build confirms the one import change was both necessary and sufficient); deployed `8f4afaf` (HTTP 200, matching commit). Playwright's dead-CDP wall keeps this a byte-identical-relocation-by-construction run (the two byte-diffs + tsc + build); the runtime-gated list-dedup stays deferred. commit `8f4afaf`. **P2-4 stays `[ ]`** — next: `KnowledgeSidebarList` (its own /knowledge-mode file) / a `Recent*` list / the context-provider trio, then the runtime-gated 6-list dedup + touch-engine unification. (Cumulative P2-4: `app-sidebar.tsx` 1778→1355, −423; five leaves into four `components/sidebar/` modules.)
- **run 34 · P2-4 (fourth leaf: `BrainSidebar` + `RecentBrainSessions` → `components/sidebar/`)** — continued the safe byte-identical phase by moving the brain-mode sidebar out of `app-sidebar.tsx` as one cohesive, contiguous unit into a new `components/sidebar/brain-sidebar.tsx`: `BrainSidebar` (the sidebar's /brain-mode swap — a "New 义脑 chat" button plus the orchestrator's own conversations, kept separate from the worker recents) and its module-private `RecentBrainSessions` (the brain-conversation list, rendered only from inside `BrainSidebar`). The pair sat in one contiguous source region (104–214, doc-comment included), single external consumer `AppSidebar`, and recon confirmed the whole block references no staying-local symbol (no `useSidebar`, no `BRAIN_NAV`/other NAV consts, no sibling `Recent*`/`KnowledgeSidebarList`/`RecentDispatchSessions`), so the move is cycle-free — note `BrainSidebar` renders only `RecentBrainSessions`, while the sibling `RecentDispatchSessions`/`KnowledgeSidebarList` are rendered by `AppSidebar` directly and stay for now. Genuine deps for the new module: react `useState`, `useSearchParams` (next/navigation), `Link` (next/link), `trpc`, `cn`, `relTime`, and lucide `SquarePen` (the `useMutation`/`useQuery` in the block are `trpc.*` methods, not standalone imports). The block byte-diffs identical against the new copy modulo the single `export` added to `BrainSidebar` (`RecentBrainSessions` intentionally stays unexported). No import prune this run, for the same real-use-vs-noise reason as run 33: a JSX-only count made `SquarePen` look block-exclusive (1 `<SquarePen>`, in-block), but it's *also* used as a plain value at `AppSidebar`'s new-chat entry — `{ href: '/chat?new=1', label: 'New chat', Icon: SquarePen }` at line 392 — so it's kept; every other dep is used across the file. `app-sidebar.tsx` **1539→1429** (git diff: 112 deletions incl. the trailing blank, 1 insertion for the import); new `brain-sidebar.tsx` is 125 lines. `tsc --noEmit` + `next build` clean; deployed `384cfc6` (HTTP 200, matching commit). Playwright's dead-CDP wall keeps this a byte-identical-relocation-by-construction run (block byte-diff + tsc + build); the runtime-gated list-dedup stays deferred. commit `384cfc6`. **P2-4 stays `[ ]`** — next: `RecentDispatchSessions` / `KnowledgeSidebarList` / a `Recent*` list / the context-provider trio, then the runtime-gated 6-list dedup + touch-engine unification. (Cumulative P2-4: `app-sidebar.tsx` 1778→1429, −349, four `components/sidebar/` modules.)
- **run 33 · P2-4 (third leaf: sidebar header buttons → `components/sidebar/`)** — continued the safe byte-identical phase by lifting the three sidebar-header icon buttons out of `app-sidebar.tsx` as one cohesive cluster into a new `components/sidebar/header-buttons.tsx`: `BrainButton` (the monochrome hermit-crab that opens the /brain orchestrator panel), `SettingsButton` (the gear → the Settings area, active on any Settings route), and `NotificationsButton` (the bell → /notifications, with the rose unread-count badge). The three sat contiguously (source 103–194, each with its own doc-comment) and are rendered only by `AppSidebar` (three call-sites), and — recon confirmed — none touches a staying-local symbol (no `useSidebar`, no `NAV`/`MARKET_NAV`/`BRAIN_NAV`/`NOTIF_FILTERS`), so the move is cycle-free. Genuine deps for the new module: `usePathname` (next/navigation), `Link` (next/link), `cn`, lucide `{Settings, Bell}`, and `{SETTINGS_HREFS, SETTINGS_ENTRY_HREF}` from `@/lib/settings-nav`. The block byte-diffs identical against the new copy modulo the three `export`s added to the function decls. The interesting part was import hygiene, where the naive counts would have caused **two** wrong prunes without the real-use-vs-noise guard: (1) `Bell` shows up in-block once (the `<Bell>` icon) but is *also* `NOTIF_FILTERS`'s `icon: Bell` at line 98 (which stays in `app-sidebar`) → **kept**; (2) `Settings` had 11 raw `\bSettings\b` hits, but only line 160 is the actual lucide icon — 152/153 are `title="Settings"`/`aria-label="Settings"` string attributes, 688 is a `>Settings</span>` text label, and the rest are comments — so the icon is genuinely block-exclusive → **pruned** from the lucide import. On the settings-nav side, a per-symbol scan showed `SETTINGS_ENTRY_HREF` is used only in the moved block → **pruned**, while `SETTINGS_HREFS` (also at `AppSidebar`'s `onSettings` ~455) and `SETTINGS_TABS` (the settings-mode vertical nav ~849) are used elsewhere → kept. `app-sidebar.tsx` **1632→1539**; new `header-buttons.tsx` is 105 lines. `tsc --noEmit` + `next build` clean — the build again proves the prunes (an over-prune of `Settings`/`SETTINGS_ENTRY_HREF` would be a hard `Cannot find name` error); deployed `a896862` (HTTP 200, matching commit). Playwright's dead-CDP wall keeps this a byte-identical-relocation-by-construction run (block byte-diff + tsc + build); the runtime-gated list-dedup stays deferred. commit `a896862`. **P2-4 stays `[ ]`** — next: a `Recent*` list / `BrainSidebar` / `KnowledgeSidebarList` / the context-provider trio, then the runtime-gated 6-list dedup + touch-engine unification.
- **run 32 · P2-4 (second leaf: `TrashedAgents` + `PurgeButton` → `components/sidebar/`)** — continued the safe byte-identical phase of P2-4 by moving the agents "recycle bin" out of `app-sidebar.tsx` as one cohesive, contiguous unit into a new `components/sidebar/trashed-agents.tsx`: `TrashedAgents` (the collapsible trash list at the bottom of the /agents sidebar — restore + purge; rendered by `RecentAgents`) and its module-private `PurgeButton` (a two-step arm→confirm permanent-delete that auto-disarms after 3.5s, used only by `TrashedAgents`). The pair sat in one contiguous source region (1062–1170, doc-comment included), single external consumer `RecentAgents` (which stays and now imports `TrashedAgents`), so — unlike the barrel API — this is a clean move with no cross-file back-reference and no cycle (verified `useSidebar` is consumed only by the staying `SidebarMobileToggle`/`AppSidebar`). Recon defused two traps before cutting: (1) the block's lower boundary — a lowercase `fmtEvery` cron-interval helper sits at 1172, immediately after `PurgeButton`, but belongs to `RecentCrons`, so the unit ends at `PurgeButton`'s close (1170), not the next decl; (2) comment-noise in the identifier scan — `ConfirmDeleteButton` (named in `PurgeButton`'s "Mirrors the header ConfirmDeleteButton" comment), plus `Recycle`/`Mirrors`/`Two` from prose, all showed up capitalised but have zero real uses (grep `<X`/`X(` excluding `//` lines confirms). Genuine deps for the new module: react `{useState, useEffect}`, `trpc`, `cn`, `relTime`, and lucide `{ChevronDown, Check, X, Trash2, RotateCcw}`. Both regions byte-diff identical against the new copy (function 1065–1170; doc-comment 1062–1064) modulo the `export` added to `TrashedAgents` (`PurgeButton` intentionally stays unexported/private). Import hygiene the other direction: a per-icon count (`<Icon` JSX uses in-file vs in-block) proved `Check`, `ChevronDown`, and `RotateCcw` were block-exclusive (1 use each, all inside the moved block), so they were **pruned** from `app-sidebar`'s lucide import; `Trash2` (also at a context-menu entry ~1566) and `X` (also another clear-button ~1608) were kept. `app-sidebar.tsx` **1740→1631**; new `trashed-agents.tsx` is 122 lines. `tsc --noEmit` + `next build` clean — and here the build is a real proof of the prune decisions, since an over-prune would surface as a hard `Cannot find name 'Check'` tsc error, not a mere warning; deployed `2981a56` (HTTP 200, matching commit). Playwright's dead-CDP wall again makes this a byte-identical-relocation-by-construction run (covered by the two byte-diffs + tsc + build); the runtime-gated list-dedup stays deferred. commit `2981a56`. **P2-4 stays `[ ]`** — next: continue the safe leaves (a `Recent*` list / `BrainSidebar` / `KnowledgeSidebarList` / the header buttons / the context-provider trio), then the runtime-gated 6-list dedup + touch-engine unification.
- **run 31 · P2-4 (first leaf: `SidebarFindInput` → `components/sidebar/`)** — opened P2-4 (decompose the 1778-line `app-sidebar.tsx`) with the run-31 layering rule: *do the safe byte-identical relocations first; defer the dedup of the 6 near-identical `Recent*` lists + the touch engine* (a real refactor that changes structure → needs runtime verification, and Playwright is still wall-dead) until a runtime-enabled session. Started with recon of the module's public surface: a scripted scan found the barrel `@/components/app-sidebar` is imported at 18 external sites, but only for `useSidebar` / `SidebarProvider` / `SidebarMobileToggle` / `AppSidebar` — so every other top-level decl (incl. the `Recent*` lists, `SidebarFindInput`, `PurgeButton`, the header buttons, `BrainSidebar`) is an internal leaf that can move without changing the public API. Picked the smallest genuine *reusable unit* first: `SidebarFindInput`, the shared sidebar search box (a controlled text input — value/onChange/placeholder/label props, Esc-clears, an inline clear button), used by `RecentAgents` and `RecentCrons`. It's fully self-contained (deps: lucide `Search`/`X` + `cn`; no hooks, no context, no page-local symbols → no cycle hazard, no shared-type lift needed), so this is a clean one-step move — unlike the chat god-file's clusters it needed no prep run. Extracted verbatim into a new `components/sidebar/sidebar-find-input.tsx` (`'use client'` + the two lucide icons + `cn` + the function with `export` prepended); the function region byte-diffs identical against source `936–970` modulo the added `export`, and its doc-comment carried over identically. In `app-sidebar` the def (comment + function + trailing blank, lines 934–971) was `sed`-deleted and a single `import { SidebarFindInput } from '@/components/sidebar/sidebar-find-input'` added; `Search`/`X` are **kept** in the file's lucide import (both still used elsewhere — `Search` in the RecentSessions search box at ~1623, `X` in `PurgeButton`/the other clear buttons), so no prune. A small bonus: `RecentAgents`'s descriptive comment had been stranded *above* `SidebarFindInput`; removing the def reunited it with its function. `app-sidebar.tsx` **1778→1740**; new module 46 lines. `tsc --noEmit` + `next build` clean; deployed `8a2d6f9` (HTTP 200, matching commit). Playwright's dead-CDP wall makes this a byte-identical-relocation run by design (covered by the byte-diff + tsc + build); the runtime-gated dedup is explicitly deferred, not skipped. commit `8a2d6f9`. **P2-4 stays `[ ]`** — next: continue the safe leaves (`PurgeButton` / a `Recent*` list / `BrainSidebar` / the provider trio), then the runtime-gated list-dedup + touch-engine unification.
- **run 30 · P2-3 (assessment + decision — DECOMPOSITION COMPLETE, timing-core retained)** — no code change this run; a deliberate stop-and-decide on `SessionPane`. State of play: `page.tsx` is down to **1200 lines** (from 3502, −66%), holding only `ChatPage` (Suspense wrapper), `ChatPageInner` (URL routing / session selection), the `SessionPane` core (line 245→end, ~955 lines, ~51 hooks), and a few staying helpers (`rowSig`/`mergeMessagesById` merge, `useIsoLayoutEffect`, `LOOP_TEMPLATE`/`CRON_TEMPLATE`, the message-filter). The 13 extracted `components/chat/*` modules cover the spec's `components/message/*` deliverable in full. **The decision: the 3 named hooks (`useSessionStream`/`useStickyScroll`/`useOutboundState`) and the 4-optimistic-overlay collapse are consciously NOT extracted, and P2-3 is marked done on that basis.** Reasoning: every one of runs 14–29 was a *byte-identical relocation* — behaviour preserved *by construction*, provable with a byte-diff + tsc + build even with Playwright dead all session. A hook extraction is categorically different: it moves state ownership across a boundary, re-times effects, re-captures closures — behaviour preserved only by careful reasoning, and the concern in question is exactly the stream/SSE/scroll/queue/working timing that is famously regression-prone (see `hermit-ui-working-detection`; the stopped loop `851d1000` was entirely this class of bug). That refactor *needs* runtime verification (send/stream/queue/scroll parity — the P2-3 test line says so), and the runtime/Playwright harness has been wall-dead 18 consecutive times this session. A timing-critical refactor with no runtime test would directly violate the loop's behaviour-preserving + self-test-every-iteration guardrails — so the honest, safe call is to keep the cohesive ~955-line live-session core intact (also the better design: three hooks sharing ~8 timing refs would need wide interfaces that *reduce* clarity). Two residuals are recorded, not forced: the 6 remaining `any`s (all the `content: any` message-filter — load-bearing untyped DB JSON, safer to tighten with P3-1's type pass) and the pre-existing unused `Button` import (P3-2). **Recommended follow-up (flagged for sway / a runtime-enabled session):** extract `useStickyScroll` first (least timing-coupled of the three) with live scroll/stream parity testing, once Playwright/runtime is restored. Net P2-3 across runs 12–30: `chat/page.tsx` 3502→1200, 13 cohesive modules, zero behaviour change, every step deployed green. **✅ P2-3 COMPLETE (scoped).** Next: P2-4 (`app-sidebar.tsx` 1778 — recon done: ~18 components incl. the 6 near-identical `Recent*` lists + `AppSidebar`'s ~494-line body; extraction starts run 31).
- **run 29 · P2-3 (composer cluster → `composer.tsx`)** — moved the last leaf, the composer cluster, out of `page.tsx` as a single unit into a new `components/chat/composer.tsx`. Exports (both consumed by `SessionPane`): `ComposeBar` (the ~480-line input box — textarea, send, attachment upload/paste/drop, slash-command suggestions) and `QueueBar` (the queued-messages strip). Module-private (referenced only within the cluster): `AttachmentChip`, `readyLabel`, `getExt`, `readImageDims`, `SLASH_COMMANDS`, and the `SAFE_FILE_EXTS`/`SAFE_FILE_EXT_SET`/`FILE_ACCEPT`/`MAX_IMAGES`/`MAX_FILES` file constants — the recon surfaced `getExt` and `readImageDims`, two upload helpers the run-28 member list had missed (both used only inside `ComposeBar`). The cluster lived in two source regions — `SLASH_COMMANDS` (which sits *before* the staying `LOOP_TEMPLATE`/`CRON_TEMPLATE`, so those were threaded around) and the contiguous `QueueBar`…`AttachmentChip` block running to EOF — excised in one `sed` multi-range pass; both regions byte-diff identical against their new copies (the 632-line region included) modulo the `export`s added to `QueueBar`/`ComposeBar`. The run-27 import methodology paid off again: the scripted classify + exhaustive capital-identifier scan caught the composer-only React event types (`ChangeEvent`/`ClipboardEvent`/`DragEvent`, used in ComposeBar's onChange/onPaste/onDrop), confirmed `React.Dispatch`/`SetStateAction`/`RefObject` resolve via the global `React` namespace (no import), and — the comment-noise guard again — unmasked `isTouchPrimary` (its only "outside" hit was the line-33 comment) and the lucide icons `X`/`Plus`/`ArrowUp`/`FileText` plus `ImageLightbox` as cluster-only, so all were pruned from `page.tsx`. New module imports react `{useState, useRef, useCallback, useEffect, useMemo, type ChangeEvent, type ClipboardEvent, type DragEvent}`, `cn`, `trpc`, `getActiveKey`, `isTouchPrimary`, `QUEUE_LIMIT`, `ImageLightbox`, lucide `{Plus, ArrowUp, FileText, X}`, lib `{msgText, type Attachment}`; `page.tsx` dropped the 3 event types, the 4 lucide icons, and the `ImageLightbox`/`isTouchPrimary` lines. Side-finding: `Button` is now grep-confirmed **unused** in `page.tsx` — a pre-existing leftover (an earlier move took its last consumer) that eslint tolerates as a warning; left untouched here (out of scope) and flagged for the P3-2 dead-code pass. `page.tsx` **1849→1201** (from the original 3502, −66%); new `composer.tsx` is 666 lines. `tsc --noEmit` + `next build` clean; deployed `ee2cc32` (HTTP 200, matching commit). Playwright hit the same dead-CDP wall an eighteenth time — deferred; both regions' byte-diffs + tsc + build cover the move. commit `ee2cc32`. **P2-3 stays `[ ]`** — beyond `ChatPage`/`ChatPageInner`/`SessionPane`, `page.tsx` now holds only a handful of helpers/consts; run 30 assesses the `SessionPane` core (stream/SSE/queue/working-timing hooks) — the likely conclusion is that it stays as the page's main component and **P2-3 is declared done**, rather than fragment the tightly-coupled core.
- **run 28 · P2-3 (lift `Attachment` type → `lib.ts` — prep for the composer cluster)** — the last leaf before the `SessionPane` core is the composer cluster: `ComposeBar` (the ~480-line input box — textarea + send + attachments + slash-command suggestions), `QueueBar` (the queued-messages strip), `AttachmentChip` + its `readyLabel` helper, and the file/slash consts `SLASH_COMMANDS` / `SAFE_FILE_EXTS` / `SAFE_FILE_EXT_SET` / `FILE_ACCEPT` / `MAX_IMAGES` / `MAX_FILES`. Assessment this run: (a) all members are module-scope → prop-driven, safe to relocate together; (b) `LOOP_TEMPLATE`/`CRON_TEMPLATE` are used *only* by `SessionPane` (passed to `LoopBar`'s `onStartLoop`/`onStartCron`), so they **stay** — even though `SLASH_COMMANDS` sits right next to them and moves; (c) the one shared page.tsx-local symbol is the `Attachment` upload type, referenced by both the composer (`ComposeBar` props, `AttachmentChip`, `readyLabel`) and `SessionPane` (which owns the `useState<Attachment[]>` state and threads it down). That shared type is the same cycle hazard `Block` was in run 26. The composer is ~620 lines across interleaved regions, so pairing the type-lift with the full move in one run is too much surface for one safe step; this run does the prep alone (the run-26 pattern): `type Attachment` (the `uploading`|`ready`|`error` discriminated union) lifted verbatim into the neutral no-React `components/chat/lib.ts` as an `export type`, and `page.tsx` now imports it from `lib`. Byte diff of the union against the new copy is identical modulo the added `export`. `page.tsx` 1856→1849 *(note: run 27's entry rounded to 1860 — that was its post-`sed` count before the 5 import-line prunes; the true post-run-27 figure is 1856, so the cumulative is 3502→1849)*. `tsc --noEmit` + `next build` clean; deployed `7d38a0e` (HTTP 200, matching commit). Playwright hit the same dead-CDP wall a seventeenth time — deferred; a one-type lift is fully covered by tsc's cross-module resolution + build's eslint-no-unused. commit `7d38a0e`. **P2-3 stays `[ ]`** — next: move the now-unblocked composer cluster (`ComposeBar`/`QueueBar`/`AttachmentChip` + consts) into `components/chat/composer.tsx`, then the `SessionPane` core hooks (last).
- **run 27 · P2-3 (message-render cluster → `message-timeline.tsx`)** — with run 26 having lifted the two shared symbols out of the way, moved the entire message-render cluster out of `page.tsx` as a single unit into a new `components/chat/message-timeline.tsx`. Members (all verbatim): `MessageTimeline` (memo, day-grouped list — the **only** `export`, since it's the only one `SessionPane` calls), and — module-private because referenced only from within the cluster — `MessageRow` (memo), `MessageActions`, `GroupView`, `HarnessTerminatorRow`, plus `type Group` / `imageSourceToUrl` / `groupConsecutiveTools`. They form a call graph (`MessageTimeline`→`MessageRow`→`GroupView`; `MessageRow`→`groupConsecutiveTools`→`imageSourceToUrl`), so like run 22's pair they had to move together or the split would cut a live reference. The cluster sat in two non-contiguous source regions (`HarnessTerminatorRow`+`MessageTimeline` near line 1200; `MessageRow`…`GroupView` from 1955 to EOF), excised in one `sed` multi-range pass; each region byte-diffed against its new copy is identical (modulo the `export` added to `MessageTimeline`). The hard part was the import graph. A scripted pass classified every imported symbol as used inside-the-cluster / outside / both; then an exhaustive scan of every capitalized identifier in the cluster caught two the first pass had missed — `TimeAgo` (used by `MessageActions`) and `React.ReactNode` (resolves via the global `React` namespace, needs no import) — and unmasked a trap: the classifier's "both" verdict for `memo` and `StreamingDots` was **comment** noise (`memo(MessageRow)` appears in prose; the only real `memo()` calls and `<StreamingDots>` uses are inside the cluster), so both had to be pruned from `page.tsx`, not kept. Final imports — new module: react `{memo, useState, useCallback}`, `cn`, `relTime`, `TimeAgo`, lib `{isSameDay, isHarnessTerminator, type Block}`, message-bits `{StreamingDots, TypedText, DateDivider}`, the four tool-chips, `InteractionCard`, file-preview `{ChatImage, ChatFile}`; `page.tsx` dropped `memo` from its react import, deleted the now-unused `relTime`/`TimeAgo`/tool-chips/interaction-card/file-preview import lines outright, and narrowed lib→`{msgText, isHarnessTerminator}` and message-bits→`{TypingIndicator}`. `page.tsx` **2266→1860** — first time below 2000, down from the original 3502; the new `message-timeline.tsx` is 424 lines. `tsc --noEmit` + `next build` clean — with a move this large the build's import-resolution + eslint-no-unused is the real proof that all ~15 keep/prune/narrow decisions were exactly right; deployed `2203d17` (HTTP 200, matching commit). Playwright hit the same dead-CDP wall a sixteenth time — deferred; a byte-identical relocation of prop-driven presentational components is fully covered by the region byte-diffs + tsc + build. commit `2203d17`. **P2-3 stays `[ ]`** — remaining: the composer (`ComposeBar` / `AttachmentChip` / `QueueBar`), then the `SessionPane` core hooks (last).
- **run 26 · P2-3 (lift `Block` + `isHarnessTerminator` → `lib.ts` — prep for the message-render cluster)** — the biggest non-core piece left in `page.tsx` is the message-render cluster: `MessageTimeline` (memo, day-grouped list) → `MessageRow` (memo, one message's blocks) → `GroupView` (tool/content groups), plus `MessageActions`, `HarnessTerminatorRow`, and the private `type Group` / `imageSourceToUrl` / `groupConsecutiveTools`. All are module-scope declarations, so they *cannot* close over `SessionPane`'s local state/hooks — they're inherently prop-driven and safe to relocate together (they call each other, so like run 22's pair they must move as a unit to avoid a cycle). The only thing in the way: the cluster references two symbols defined at `page.tsx` module scope that a *staying* helper (the assistant-turn message filter near the top of the file) **also** uses — the `Block` content-block type and the pure `isHarnessTerminator` predicate (detects Claude Code's "No response requested." JSONL terminator turns). If the cluster moved out with those left behind, the new module would import them from `page.tsx` while `page.tsx` imports the cluster — a `page.tsx`↔module cycle. This run removes that obstacle with the same neutral-home move used for the date helpers in run 25: both were lifted verbatim into `components/chat/lib.ts` (no React, no JSX — a `type` and a pure function both qualify), `export`ed (the predicate keeping its explanatory doc-comment), and `page.tsx` now imports `{ isHarnessTerminator, type Block }` from `lib` alongside `msgText`/`isSameDay`. `HarnessTerminatorRow` (a JSX component) was deliberately *not* lifted — it belongs to the render cluster and moves with it next run. Byte-level diffs of both lifted symbols against their new copies are identical modulo the added `export`. `page.tsx` 2297→2266. `tsc --noEmit` + `next build` clean; deployed `bd9ec34` (HTTP 200, matching commit). Playwright hit the same dead-CDP wall a fifteenth time — deferred; two byte-identical lifts (a type + a pure predicate) are fully covered by tsc's cross-module resolution + build's eslint-no-unused. commit `bd9ec34`. **P2-3 stays `[ ]`** — next: move the now-unblocked message-render cluster as a unit, then the composer (`ComposeBar`/`AttachmentChip`/`QueueBar`), then the `SessionPane` core hooks.
- **run 25 · P2-3 (`isSameDay`/`ymdLocal` → `lib.ts` + DateDivider out of the god-file)** — resolved run 24's deferred blocker and finished the `DateDivider` extraction in a single run, in two verbatim moves. **(1)** The two pure, React-free date helpers — `ymdLocal` (localized `weekday, month day, year` label) and `isSameDay` (same-calendar-day compare) — were lifted out of `page.tsx` module scope into `components/chat/lib.ts` (the no-React helper module started in run 17 for `msgText`), now `export`ed. They have two consumers in different places: `MessageTimeline`'s day-grouping loop (stays in `page.tsx`) and the `DateDivider` label — so a shared neutral home is exactly what lets `DateDivider` move without `page.tsx` and the new module importing each other (the cycle that made run 24 fall back to `RestartBar`). **(2)** `DateDivider` (the centered Today / Yesterday / date separator between day groups) moved into `components/chat/message-bits.tsx`, verbatim; the receiving file gained `useMemo` (React) and `{ isSameDay, ymdLocal }` from its sibling `./lib`. Import tracking on `page.tsx`: `isSameDay` was added to the existing `lib` import (still used by `MessageTimeline`), `DateDivider` to the existing `message-bits` import — and `ymdLocal` was deliberately **not** imported, because `DateDivider` was its only user and has now left (`page.tsx` importing it would have tripped `@typescript-eslint/no-unused-vars` and failed the build — the same guard that has caught every prior prune). Byte-level diffs of both moved clusters against their new copies are identical modulo the added `export`s. `page.tsx` 2325→2297. `tsc --noEmit` + `next build` clean (the build's eslint-no-unused is the proof the three-file import graph is exactly right); deployed `de864e9` (HTTP 200, matching commit). Playwright hit the same dead-CDP wall a fourteenth time — deferred; two byte-identical moves (pure functions + a presentational component) are fully covered by tsc's cross-module contract check + build's import-resolution/JSX/eslint. commit `de864e9`. **P2-3 stays `[ ]`** — remaining: the message-render cluster (`MessageRow`/`MessageTimeline`/`GroupView`), the composer (`ComposeBar`/`AttachmentChip`/`QueueBar`), then the `SessionPane` core hooks.
- **run 24 · P2-3 (RestartBar out of the god-file)** — extracted `RestartBar` — the docked bottom bar that reads "This session isn't active." with a "Restart to continue" button — from `chat/page.tsx` into a new `components/chat/restart-bar.tsx`, verbatim (its multi-line "why" doc-comment moved with it). This run's intended target was `DateDivider`, but the run-24 hard-gate check blocked it: `DateDivider`'s date labels come from two `page.tsx` module-scope helpers, `isSameDay` and `ymdLocal`, which `MessageTimeline` (staying in `page.tsx`) **also** calls — so lifting `DateDivider` out on its own would leave `page.tsx` importing those helpers back from the new module while the module still needs them too, i.e. a `page.tsx`↔module cycle. The gate's prescribed move is to lift those pure helpers into `components/chat/lib.ts` first (a future run) and only then relocate `DateDivider`; the sanctioned fallback for this run is `RestartBar` (props-only `{restarting, onRestart}`, sole dependency `Button`, single `SessionPane` consumer, zero cross-refs, no imports to prune). **Honest caveat — `RestartBar` is dead code:** its render is gated by `const showRestartBar = inactive && !restartArmed` with `inactive` hardcoded `false`, so the `<RestartBar>` branch never executes (this is exactly the P3-2 dead-code entry). That makes the relocation behavior-neutral *by construction* — a component that never renders renders no differently from its own file — and makes the eventual P3-2 cleanup strictly easier: delete one self-contained module + its import + the `showRestartBar` branch, instead of untangling an inline function from `SessionPane`. The P3-2 backlog line was updated to point at the new module. Byte-level diff of the removed function against the new copy is identical modulo the added `export` + the module doc header. `page.tsx` 2351→2324. `tsc --noEmit` + `next build` clean; deployed `8836e5e` (HTTP 200, health check reports the matching commit). Playwright hit the same dead-CDP wall a thirteenth time — deferred; the byte-identical move of a never-rendered presentational component is fully covered by tsc (the `{restarting, onRestart}` contract typechecks at the `SessionPane` call site) + build's import-resolution/JSX/eslint-no-unused. commit `8836e5e`. **P2-3 stays `[ ]`** — message-render cluster, composer, `DateDivider` (after the `isSameDay`/`ymdLocal` helper-lift), then the `SessionPane` core: several runs to go.
- **run 23 · P2-3 (TypedText + useTypewriter out of the god-file)** — appended the streaming-tail typewriter reveal to the same `components/chat/message-bits.tsx` (started run 22): `TypedText`, which renders the assistant's text plain char-by-char while the block is still "typing" and settles into rendered `Markdown` once it's fully shown, plus its private `useTypewriter` hook (a `requestAnimationFrame` ease-out reveal that honours `prefers-reduced-motion`; only `TypedText` calls it, so it travelled along). `TypedText` is `export`ed (its one consumer, `GroupView`, stays in `page.tsx` and now imports it); `useTypewriter` stays module-private. The receiving file gained the imports these needed — `useState`/`useEffect` from React and `Markdown` — added once and de-duped against its existing `cn` import. Notably `TypedText` was the **last** `Markdown` consumer left in `page.tsx` (every other one had already moved out in earlier runs — file-preview in run 16, loop-bar in run 17), so the now-unused `Markdown` import was pruned from `page.tsx` too. A byte-level diff of the removed cluster against the appended copy is identical modulo the added `export` on `TypedText`. `page.tsx` 2387→2351. `tsc --noEmit` + `next build` clean; deployed `348f3be` (HTTP 200). Live Playwright hit the same dead-CDP wall a twelfth time — deferred; byte-identical + tsc (both symbols' contracts typecheck across the boundary) + build's import-resolution/JSX/eslint (which is what caught the `Markdown` prune) + the byte-diff. commit `348f3be`. **P2-3 stays `[ ]`** — message-render cluster, composer, `DateDivider`, `RestartBar`, then the `SessionPane` core: several runs to go.
- **run 22 · P2-3 (StreamingDots + TypingIndicator out of the god-file)** — extracted the chat timeline's two "thinking" indicators into `components/chat/message-bits.tsx`: `StreamingDots` (a single solid dot that breathes via a `motion-safe` animation; `variant` nudges its size) and `TypingIndicator` (a thin `justify-start` wrapper that renders one bubble-variant `StreamingDots`). These are a **cross-coupled pair** — `TypingIndicator` renders `StreamingDots` — that happened to live ~860 lines apart in `page.tsx`; extracting them together keeps that reference intra-module and sidesteps the circular `page.tsx`↔module import that splitting them would create (the reason run 21 deferred them). Both are `export`ed: `StreamingDots` is additionally consumed by `MessageRow` (two call sites, which stay in `page.tsx`) and `TypingIndicator` by `SessionPane` — all switched to import from `message-bits`. The two removals were done in a single `sed` pass ordered high-line-first (`StreamingDots` block first, then `TypingIndicator`) so the earlier deletion couldn't shift the later line numbers, and a stray double-blank line the removal left behind was collapsed back to one. Byte-level diffs of both removed functions against the new file's copies are identical modulo the added `export`. `page.tsx` 2410→2386. `tsc --noEmit` + `next build` clean; deployed `70c8123` (HTTP 200). Live Playwright hit the same dead-CDP wall an eleventh time — deferred; byte-identical + tsc (both components' prop contracts typecheck across the new boundary) + build's import-resolution/JSX/eslint + the byte-diffs. commit `70c8123`. **P2-3 stays `[ ]`** — message-render cluster, composer, `DateDivider`, `TypedText`+`useTypewriter`, `RestartBar`, then the `SessionPane` core: several runs to go.
- **run 21 · P2-3 (EmptyChat out of the god-file)** — extracted `EmptyChat`, the empty-state starter screen shown when a session has no messages yet (the agent's initials avatar, a "Start a chat with …" heading, and four starter-prompt cards that fill the composer via `onPickPrompt`), into `components/chat/empty-chat.tsx`, verbatim. It's fully self-contained: a single consumer (`SessionPane`), one dependency (`useMemo`), no cross-references and no shared module-scope helpers, so nothing needed pruning. Chosen as this run's surgical single-component move over the tempting "presentational bits" bundle, because those bits are cross-coupled — `TypingIndicator` renders `StreamingDots`, which lives in a separate region of the file, and splitting the pair across the module boundary would risk a circular `page.tsx`↔module import — so they're deferred to dedicated later runs. A byte-level diff of the removed function against the new file's copy is identical modulo the added `export` + the trailing separator blank. `page.tsx` 2451→2409. `tsc --noEmit` + `next build` clean; deployed `10ca6f6` (HTTP 200). Live Playwright hit the same dead-CDP wall a tenth time — deferred; byte-identical + tsc (the `EmptyChat` prop contract typechecks across the new boundary) + build's import-resolution/JSX/eslint + the byte-diff. commit `10ca6f6`. **P2-3 stays `[ ]`** — message-render cluster, composer, the `StreamingDots`+`TypingIndicator` pair, `DateDivider`, `TypedText`+`useTypewriter`, `RestartBar`, then the `SessionPane` core: several runs to go.
- **run 20 · P2-3 (ConfirmIconButton out of the god-file)** — extracted `ConfirmIconButton`, the reusable icon button with an inline two-step confirm (click arms it → ✓ confirm / ✗ cancel, auto-disarming after 3.5s), used for the destructive/disruptive session actions (restart, delete), into `components/chat/confirm-icon-button.tsx`, verbatim. It's consumed only by `SessionPane` (three call sites in its header), which now imports it. Two lucide imports the button solely owned — the `Check` icon and the `LucideIcon` type (a false grep hit remained for `Check` because the string `'Check in'` appears in `EmptyChat`'s suggestions, but the real symbol was ConfirmIconButton-only) — were pruned from the `page.tsx` lucide import so eslint no-unused-vars stays green (`cn` / `X` stay, still used elsewhere). A byte-level diff of the removed function against the new file's copy is identical modulo the added `export` + the trailing separator blank. `page.tsx` 2517→2450. `tsc --noEmit` + `next build` clean; deployed `0f1c2ea` (HTTP 200). Live Playwright hit the same dead-CDP wall a ninth time — deferred; byte-identical + tsc (the `ConfirmIconButton` `icon: LucideIcon` prop contract typechecks across the new boundary) + build's import-resolution/JSX/eslint + the byte-diff. commit `0f1c2ea`. **P2-3 stays `[ ]`** — message-render cluster, composer, small presentational bits, `RestartBar`, then the `SessionPane` core: several runs to go.
- **run 19 · P2-3 (NewChatPane out of the god-file)** — extracted the "New chat" screen — `NewChatPane` (agent picker, or locked to its one agent for a scoped share session, → `chat.createSession`) — into `components/chat/new-chat-pane.tsx`, verbatim. It's a self-contained view with a single consumer (`ChatPageInner`), which now imports it. Because `NewChatPane` was the only user of the `ui/select` family in this file, the whole `Select` / `SelectTrigger` / `SelectValue` / `SelectContent` / `SelectItem` import line is dropped from `page.tsx` (the other cluster deps — `SidebarMobileToggle` / `Plus` / `Button` / `trpc` — stay, still used elsewhere). A byte-level diff of the removed function against the new file's copy is identical modulo the added `export` + the trailing separator blank. `page.tsx` 2571→2517. `tsc --noEmit` + `next build` clean; deployed `75b8445` (HTTP 200). Live Playwright hit the same dead-CDP wall an eighth time — deferred; byte-identical + tsc (the `NewChatPane` prop contract typechecks across the new boundary) + build's import-resolution/JSX/eslint + the byte-diff. commit `75b8445`. **P2-3 stays `[ ]`** — message-render cluster, composer, small presentational bits, `ConfirmIconButton`/`RestartBar`, then the `SessionPane` core: several runs to go.
- **run 18 · P2-3 (ChatFind out of the god-file)** — extracted the in-chat find feature — `ChatFind` (the Cmd/Ctrl+F search bar) together with its module-private CSS-Custom-Highlight-API scaffolding (`HL_CTOR` / `HL_REG` / `HL_OK` / `HL_WINDOW` + the `rangeFrom` helper) — into `components/chat/chat-find.tsx`, verbatim. The whole Highlight block is used only by `ChatFind`, so it moved with it; `ChatFind` is consumed only by `SessionPane` (its single call site), which now imports it. The cluster sat mid-file (SessionPane follows), so it came out with a `sed` range delete; two lucide icons the cluster solely owned — `ChevronUp` and `ChevronDown` (the latter's only other users, `LoopCard`/`LoopRunRow`, had already left in run 17) — were pruned from the `page.tsx` lucide import so eslint no-unused-vars stays green (`Search` / `X` remain, still used elsewhere). A byte-level diff of the removed cluster against the new file's copy is identical modulo the added `export` on `ChatFind` + the trailing separator blank. `page.tsx` 2748→2570. `tsc --noEmit` + `next build` clean; deployed `4b7f046` (HTTP 200). Live Playwright hit the same dead-CDP wall a seventh time — deferred; the move is byte-identical and covered by tsc (the `ChatFind` prop contract typechecks across the new module boundary) + build's import-resolution/JSX/eslint + the byte-diff. commit `4b7f046`. **P2-3 stays `[ ]`** — message-render cluster, composer, small presentational bits, misc, then the `SessionPane` core: several runs to go.
- **run 17 · P2-3 (Loop UI cluster + shared `msgText` out of the god-file)** — two verbatim moves. (a) The pure `msgText` helper (flatten a message's content blocks → plain text) went into a new no-React `components/chat/lib.ts` — it's needed by both the `SessionPane` core that stays and the loop cluster that leaves, so it becomes a shared module. (b) The loop/schedule strip — `LoopBar` + its `LoopCard` / `LoopDetail` / `LoopRuns` / `LoopRunRow` children + the `parseLoopRun` helper + the `LoopEntry` / `LoopRun` types — went into `components/chat/loop-bar.tsx`; only `LoopBar` is consumed outside (SessionPane's single call site), the rest stay module-private, and `parseLoopRun` imports `msgText` from `./lib`. `page.tsx` keeps its own `msgText` call sites (SessionPane's optimistic-outbound matching + QueueBar's one-line preview) via an import from `./lib`, and the `LOOP_TEMPLATE` / `CRON_TEMPLATE` composer constants stay put (they're handed to `LoopBar` as `onStart*` callbacks, not referenced by the cluster). The cluster sat mid-file (QueueBar follows it), so it came out with a `sed` range delete; byte-level diffs of both removed blocks against the new files are identical modulo the added `export` on `msgText`/`LoopBar` + the trailing separator blank. `page.tsx` 3030→2748 (−289, +2 imports). `tsc --noEmit` + `next build` clean; deployed `3fb62f0` (HTTP 200). Live Playwright hit the same dead-CDP wall a sixth time — deferred; the move is byte-identical and covered by tsc (the `LoopBar` prop contract + `msgText` signature both typecheck across the new module boundary) + build's import-resolution/JSX/eslint + the byte-diff. commit `3fb62f0`. **P2-3 stays `[ ]`** — message-render cluster, composer, ChatFind, misc, then the `SessionPane` core: several runs to go.
- **run 16 · P2-3 (file-preview cluster out of the god-file)** — extracted the attachment-rendering cluster — `ChatImage` (capped thumbnail → zoomable lightbox) + `ChatFile` (agent-attached file: in-app preview overlay for html/svg/pdf/text, else share/download) + their module-private internals `TEXT_EXT` / `classifyFile` / `FilePreviewBody` — into `components/chat/file-preview.tsx`, verbatim. Both public components are consumed only by `GroupView`, which now imports them. Unlike runs 14-15 this cluster sat **mid-file** (GroupView follows it), so it was excised with a `sed` range delete rather than a `head` tail-truncate — and three imports the cluster solely owned were pruned from `page.tsx` so eslint's no-unused-vars stays green: `Overlay` (removed), `saveFile` (dropped from the `save-file` import, `isTouchPrimary` stays), `Download` (dropped from the lucide import); `ImageLightbox` / `FileText` / `Markdown` remain (still used elsewhere in the file). A byte-level diff of the removed cluster against the new file's copy is identical modulo the added `export` keywords + the file's own doc header. `page.tsx` 3201→3030. `tsc --noEmit` + `next build` clean; deployed `1d6d2ef` (HTTP 200). Live Playwright hit the same dead-CDP wall a fifth time — deferred per the loop's standing allowance; a byte-identical relocation of pure presentational components is conclusively covered by build's JSX-compile + import-resolution + eslint unused-import check + the byte-diff. commit `1d6d2ef`. **P2-3 stays `[ ]`** — message-render cluster, Loop UI, composer, ChatFind, misc, then the `SessionPane` core: several runs to go.
- **run 15 · P2-3 (interaction cards out of the god-file)** — extracted `InteractionCard` (permission/question card rendered inline from a `{type:'interaction'}` content block) + its internal `QuestionCard` (option buttons + free-text "Other") into `components/chat/interaction-card.tsx`, verbatim. They were the new tail block of `chat/page.tsx` after run 14, with no shared state or timing coupling to `SessionPane`; `InteractionCard`'s one cross-cut dependency `oneLineArg` already lives in the sibling `tool-chips.tsx` (run 14), so the move is a clean leaf excision. The `page.tsx` diff is exactly the tail cluster removed + the import line swapped (`oneLineArg`, now unused there, dropped from the tool-chips import; `InteractionCard` imported from the new module) — a byte-level diff of the removed cluster against the new file's copy is identical modulo the added `export` + the file's own doc header. `page.tsx` 3361→3200. `tsc --noEmit` + `next build` clean; deployed `d219b3c` (HTTP 200). Live Playwright hit the same dead-CDP wall a fourth time (MCP bound to an endpoint with no Chrome, no forbidden mid-session restart) — deferred per the loop's standing allowance; a byte-identical relocation of a pure presentational component is conclusively covered by build's JSX-compile + import-resolution + the byte-diff. commit `d219b3c`. **P2-3 stays `[ ]`** — message-render, Loop UI, composer, ChatFind, misc, then the `SessionPane` core: several runs to go.
- **run 14 · P2-3 (started — tool-chip cluster out of the god-file)** — first, safest carve of the 3502-line `chat/page.tsx`. Mapped its ~40 components, then extracted the self-contained tool-call rendering cluster (`ToolChip` / `ToolBatchChip` / `InlineToolResult` / `InlineToolResultBatch` + the `oneLineArg` formatter, also consumed by `InteractionCard`, + private helpers `chipSurface`/`shorten`/`shortenPath`/`firstLine`/`extractToolResultText`) into `components/chat/tool-chips.tsx` verbatim. It's a contiguous block at the file's tail with no shared state or timing, so behaviour is identical — the `page.tsx` diff is exactly one import added + the block moved out (3502→3361). `tsc --noEmit` + `next build` clean; deployed `4c7a352` (HTTP 200). Live Playwright deferred again (the session's MCP is bound to a dead CDP endpoint — third run hitting the same wall, unrecoverable without a forbidden mid-session restart); a byte-identical relocation of pure presentational components is conclusively covered by build's JSX-compile + import-resolution. Leaf-first strategy: safe clusters out before `SessionPane`'s risky stream/queue/working core. commit `4c7a352`. **P2-3 stays `[ ]`** — message-render, interaction, Loop UI, composer, ChatFind, misc, then the core: several runs to go.
- **run 13 · P2-2 (DONE — shared right pane, de-fork complete)** — extracted the explorer's entire right pane into `file-explorer/file-pane.tsx`: shared `FilePane` + `FileContent`/`TextPreview`/`ImagePreview`/`FileEditor`/`DownloadBtn`, source-parametrized, with a `capabilities={{ authoring }}` flag for the global-memory folder's in-browser edit/save + empty-file Edit prompt (the only real divergence; the couple of copy strings that tracked it branch on it, minor drifted cosmetic copy unified). Both parents now memoize `source` (useMemo) so `ImagePreview`'s source-dependent effect can't refetch every render — the one genuine runtime hazard, fixed proactively. Kept two thin surface shells composing the shared blocks (tree + right pane + core) rather than one mega-component: the residual per-surface code — upload Toolbar (agent) vs new-file Toolbar + `NotePane` (global) — is genuine capability difference, not duplication, and the brief endorsed thin wrappers over over-abstraction. Surface components **1201→429 lines** across runs 12+13 (shared module 503). `tsc` + `next build` clean; deployed `77113cc` (HTTP 200); server-path probes for BOTH sources cover the full contract now that the right pane is shared — `list` (browse) **and** `readText` (preview/edit-load): agent `asst` 200/108 + readText 2451 chars, globalMemory 200/2 + readText 1522 chars, all with the exact `srcInput` inputs. ⚠️ Live browser click-through **stayed infra-blocked** (session Playwright MCP bound to a dead CDP endpoint, unrecoverable without a forbidden mid-session `claude mcp` restart — same wall as run 12); recorded as an environmental defer per the loop's standing allowance, with a suggested manual spot-check. commit `77113cc`. **✅ P2-2 COMPLETE (runs 12+13).**
- **run 12 · P2-2 (partial — shared file-explorer foundation + fmtSize fix)** — `agent-files.tsx` and `global-memory-files.tsx` were ~80% copy-paste. Extracted the capability-independent foundation into `components/file-explorer/`: `core.ts` (the `FileSource` abstraction producing the flat `{agentName|globalMemory}` discriminant every fileManager call needs + the pure helpers + the source-parametrized prepared-download path) and `file-tree.tsx` (the lazy `<FileTree source= emptyLabel= …/>` that was each file's private `TreeChildren`/`TreeNode`, identical but for the list-query source). **Fixed the named `fmtSize` drift** — global-memory stopped at MB, agent went to GB; one canonical GB-capable impl now (identical output for every file < 1 GB). Both components consume the shared module; they drop 273 lines of duplication (556→415, 645→513) for 190 shared lines. Behaviour-preserving (same JSX/hooks/tRPC; plain `<a download>`). `tsc --noEmit` + `next build` clean; deployed `8cd735a` (HTTP 200); live server-path probes confirm both `fileManager.list` sources answer 200+entries for the exact inputs `srcInput` now builds (agent `asst` → 108, globalMemory → 2). ⚠️ Real-browser Playwright pass **couldn't run** — the shared MCP/CDP browser backend was wedged (bound to a dead endpoint; no clean restart mid-session), so the click-through of both faces is deferred to run 13; the shared `<FileTree>`+`core` are still covered because both surfaces use them and the server-probe exercised both source discriminants. commit `8cd735a`. **P2-2 stays `[ ]`** — Toolbar/FilePane/NotePane unification into the full `<FileExplorer>` + the deferred browser test remain.
- **run 11 · P2-1 (DONE — `gatewayProcedure` alias + tenancy static-scan test)** — closed out P2-1's remaining (b)+(c). **(b)** Added `export const gatewayProcedure = machineProcedure` in `trpc.ts` (byte-identical enforcement — rejects scoped `shr_` keys, `ctx.machine` intact) and migrated the 22 machine-wide gateway poll/ack/`*ForGateway` endpoints to it across chat(10)/cron(1)/agents(3)/fileStation(2)/machines(2)/knowledge(2)/skills(2). Pure intent clarity: "gateway plumbing the machine's daemon drains" now reads distinctly from a browser-facing full-access endpoint, and it's one seam for future gateway-only policy. Browser-driven machine actions (`reapIdleNow`, `hosts.ackAlert`) deliberately **stay** `machineProcedure`. **(c)** A zero-runtime-cost construction-enforcement guard was found infeasible (two legitimate scoping shapes — auto-asserted `agentName`/`name` input vs. post-load `ctx.assertAgent`; getter-tracking is fragile under tRPC ctx merging), so it was **descoped to a static-scan regression test** `apps/gateway/src/tenancy.test.ts`: it slices every `agentProcedure` endpoint across the 6 agent-facing routers and asserts each contains ≥1 scoping token (`assertAgent`/`scopedAgent`/`fsTarget`/`agentName`/`name: z.string`), resolving named input schemas (`CronInput`) and guarding against a vacuous pass (≥25 endpoints scanned). It encodes the run-10 audit as a durable invariant — GREEN on current code, and would have failed on the pre-fix `downloadStatus`. During authoring it correctly flagged `cron.create` (false positive from the `CronInput` indirection), which drove the schema-resolution step — evidence it has teeth. Verified: gateway 54/54 tests + typecheck; dashboard typecheck + `next build` clean; deployed `e2261d3` (HTTP 200); runtime probe of migrated endpoints — **200 with machine key, 401 without/bogus** (gateway polls unaffected, auth intact). commit `e2261d3`. **✅ P2-1 COMPLETE (runs 10+11).**
- **run 10 · P2-1 (partial — download-scope leak fixed + surface audited)** — a full tenancy audit (subagent-enumerated, then verified by hand) of all ~30 id-keyed `agentProcedure` endpoints found **one** deviation from the assert-ownership pattern: `fileManager.downloadStatus` (id-keyed by `dl_<uuid>`) checked only `machineId`, so a scoped `shr_` share key could read a sibling agent's prepared-download metadata (status/filename/size) and, with the id, its bytes. Low severity (unguessable random id, from the caller's own `fsTarget`-gated `prepareDownload`), but the one gap. Fixed by stamping the owner agent on the in-memory download record (`DownloadEntry.agentName` + `createDownload(id, machineId, agentName)`, mutated-in-place so it survives `markDownloadReady`) and enforcing it for scoped keys in both `downloadStatus` and `/api/file-manager/download/<id>` — machine keys unchanged (checks gated on `scope==='agent'`). No DB/migration (in-memory stash). **`market.ts` ownership → declined** (documented fleet-wide shared registry; `machineProcedure` already blocks scoped keys → single-tenant). Every other id-keyed endpoint verified already-asserting, zero assert-too-late. dashboard typecheck + build clean; deployed `7eee86b` (HTTP 200); smoke-tested post-deploy (byte route: machine-key+bogus→404, no-key→401, `downloadStatus`+bogus→200 graceful-error — no 500, machine path un-regressed). commit `7eee86b`. **P2-1 stays `[ ]`** — `gatewayProcedure` alias + construction-enforcement (regression prevention) remain.
- **run 9 · P1-5 (Part B step 1 — DONE)** — closed the last working-detection desync: the snapshot collector saw `tool-running` (it passes the tail to `sessionActivity`) but the `deliverMessages` idle gate (`chat-runner.ts:531/533`) did not, so a queued batch could be injected INTO a long quiet mid-tool turn on a narrow pane. `sessionActivity` now derives tool-running from EITHER the caller's pre-read tail (`transcriptLines`, the snapshot — no double read) OR a bounded tail it reads itself from `transcriptPath` (new sync `readTranscriptTail()`, byte-bounded like `newestLineIsTurn`: a mid-flight `tool_use` is always at the file's end and any `tool_result` for it is even newer → a window reaching the `tool_use` can't miss its result, so no false "running"). The delivery gate already passes a path ⇒ **zero call-site change**. Scope-limited: cron already tracks its own `toolsOut/toolsBack`, reaper/restart pass no path (pure pane-marker, unchanged). Bias toward "busy" (hold) is the safe direction; a stuck `tool_use` self-heals via the 20-min cap + the gate's `tmuxSessionExists` drops dead panes. +3 unit tests (`readTranscriptTail` + path-driven tool-running); 47 total green. gateway typecheck clean; runtime-verified: restart (pid 66490→95707), chat-tick + snapshots steady `ok` (no crash from the internal-read path), live parity 14-pane sample classified correctly (2 genuinely-working incl. this session, 12 idle). commit `9c25006`. **✅ P1-5 COMPLETE (runs 7+8+9).**
- **run 8 · P1-5 (Part B step 2, partial)** — unified the uuid-drift adoption logic. The chat reattach path (`chat-runner.ts`) and the cron freshly-spawned path (`cron-runner.ts`) each open-coded the same "adopt the newest unclaimed live transcript" pick; extracted one shared helper in `@hermit-ui/tmux-driver`: **`pickLiveTranscript(transcripts, opts, now)`** (PURE — newest non-empty transcript whose uuid ∉ `exclude`, within `[minMtimeMs, age<maxAgeMs]`) + `resolveLiveTranscript(cwd, opts)` (fs+clock wrapper) + a `TranscriptInfo` type. chat passes `exclude = {recorded uuid} ∪ sibling live uuids`, `maxAgeMs = recorded ? FRESH_MS : undefined` (**unbounded when the recorded uuid was pruned — the 2026-07-13 stuck-on-`starting` fix, preserved exactly**) and reuses its already-read `listTranscripts()` (no second dir read); cron passes `exclude = pinnedUuids`, `minMtimeMs = startedAt - 2s`. Each old filter maps 1:1 onto the helper (undefined bound ≡ the old `Infinity`/absent predicate) ⇒ **behavior-preserving**. +7 unit tests (each exclusion source + bound), 44 total green; gateway + tmux-driver typecheck clean; runtime-verified: **restart re-ran every session's reattach path through the refactored helper** (pid 69896→66490) with zero wiring/crash errors, snapshots + chat-tick steady `ok`. commit `ba00135`. **P1-5 stays `[ ]`** — Part B step 1 (delivery-gate tool-running desync) pending.
- **run 7 · P1-5 (Part A, partial)** — unified the working-detection composition behind one verdict. New `sessionActivity(sessionId, opts): {working, reason}` in `pane.ts` ORs all four signals (transcript-fresh / tool-running / pane-marker / hook-active) cheapest-first and names the winner; extracted `capturePaneMarker()` (one capture → `{marker, cols}`) from the old inline body; **moved `transcriptToolRunning` + `TOOL_RUNNING_CAP_MS` from the snapshot collector into `pane.ts`** so the "tool in flight" signal is composed inside the verdict instead of bolted onto the one snapshot call site. `paneIsWorking` is now a **zero-logic boolean alias** over `sessionActivity` → the send/queue/cron gates stay byte-identical (no `transcriptLines` ⇒ tool-running inert). The snapshot routes through `sessionActivity`, passing the tail it already reads ⇒ **same ORed condition set = behavior-identical**. +8 unit tests (transcriptToolRunning caps/ordering + sessionActivity short-circuits), 37 total green; gateway typecheck clean; runtime-verified: restart (pid 5445→69896), `[session-snapshots]` settled to steady `ok` (~600ms, ran sessionActivity for all 22 live panes, no crash), + live parity spot-check (12-pane sample: only the genuinely-working pane — this loop's own session — read WORKING, 11 idle read idle). commit `39dab27`. **P1-5 stays `[ ]`** — Part B (gate desync closure + `resolveLiveTranscript()` drift-adopt) pending.
- **run 6 · P1-4** — tightened the two highest-traffic gateway→dashboard sync routes off `z.any()`: `chat-message` `content` → `z.union([z.string(), z.array(z.unknown())])`; `session-snapshot` `state` → `z.enum(['starting','working','idle']).nullable()`, `loopState` → `z.unknown()`. **Deliberately permissive** because one bad item 400s the *whole* batch — verified every real producer stays inside the new schema before tightening: both `content` producers (chat-runner `normalizeContent` + mcp-stub attach `blocks`) always emit arrays; the collector's `state` is exactly `{null,'starting','working','idle'}` (session-snapshot.ts:242/257/260); `loopState` is opaque agent JSON (kept unvalidated, `z.unknown()` stays optional). Content cast to the column's JSON type at the Prisma boundary (same idiom as routers/chat.ts). Dashboard typecheck + `next build` green; deployed `129d42b` → **runtime-verified: 0× `→ 400` in the entire gateway err.log, `[session-snapshots] ok` every 8s + `[chat-tick] ok` post-deploy** (the only err.log noise was the expected 502 blip during the dashboard restart). No shared cross-side schema module yet (the routes live in the dashboard, the producers in the gateway/mcp-stub `.cjs` — a shared package is a P3-scale move); left as a typed-per-route tightening.
